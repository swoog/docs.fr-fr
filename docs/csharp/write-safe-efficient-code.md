---
title: Écrire du code C# sécurisé et efficace
description: Les améliorations récentes apportées au langage C# vous permettent d’écrire du code sécurisé vérifiable que les performances associaient précédemment à du code non sécurisé.
ms.date: 10/23/2018
ms.custom: mvc
ms.openlocfilehash: d363e357d3749bb2014456c0064c4de7dd7f1acb
ms.sourcegitcommit: 41c0637e894fbcd0713d46d6ef1866f08dc321a2
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/01/2019
ms.locfileid: "57411564"
---
# <a name="write-safe-and-efficient-c-code"></a><span data-ttu-id="c32a3-103">Écrire du code C# sécurisé et efficace</span><span class="sxs-lookup"><span data-stu-id="c32a3-103">Write safe and efficient C# code</span></span>

<span data-ttu-id="c32a3-104">Les nouvelles fonctionnalités de C# vous permettent d’écrire du code sécurisé vérifiable avec de meilleures performances.</span><span class="sxs-lookup"><span data-stu-id="c32a3-104">New features in C# enable you to write verifiable safe code with better performance.</span></span> <span data-ttu-id="c32a3-105">Si vous appliquez soigneusement ces techniques, les scénarios nécessitant du code non sécurisé sont moins nombreux.</span><span class="sxs-lookup"><span data-stu-id="c32a3-105">If you carefully apply these techniques, fewer scenarios require unsafe code.</span></span> <span data-ttu-id="c32a3-106">Ces fonctionnalités facilitent l’utilisation des références à des types valeur comme arguments de méthode et retours de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-106">These features make it easier to use references to value types as method arguments and method returns.</span></span> <span data-ttu-id="c32a3-107">Quand elles sont utilisées de façon sécurisée, ces techniques réduisent la copie des types valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-107">When done safely, these techniques minimize copying value types.</span></span> <span data-ttu-id="c32a3-108">En utilisant des types valeur, vous pouvez réduire le nombre d’allocations et de passes du garbage collection.</span><span class="sxs-lookup"><span data-stu-id="c32a3-108">By using value types, you can minimize the number of allocations and garbage collection passes.</span></span>

<span data-ttu-id="c32a3-109">Une grande partie de l’exemple de code de cet article utilise des fonctionnalités ajoutées dans C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="c32a3-109">Much of the sample code in this article uses features added in C# 7.2.</span></span> <span data-ttu-id="c32a3-110">Pour utiliser ces fonctionnalités, vous devez configurer votre projet pour qu’il utilise C# 7.2 ou ultérieur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-110">To use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="c32a3-111">Pour plus d’informations sur la définition de la version du langage, consultez [Configurer la version du langage](language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="c32a3-111">For more information on setting the language version, see [configure the language version](language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="c32a3-112">Cet article met l’accent sur les techniques de gestion efficace des ressources.</span><span class="sxs-lookup"><span data-stu-id="c32a3-112">This article focuses on techniques for efficient resource management.</span></span> <span data-ttu-id="c32a3-113">Un avantage de l’utilisation de types valeur est qu’ils permettent souvent d’éviter les allocations de tas.</span><span class="sxs-lookup"><span data-stu-id="c32a3-113">One advantage to using value types is that they often avoid heap allocations.</span></span> <span data-ttu-id="c32a3-114">Mais l’inconvénient, c’est qu’ils sont copiés par valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-114">The disadvantage is that they're copied by value.</span></span> <span data-ttu-id="c32a3-115">Ce compromis complique l’optimisation des algorithmes qui opèrent sur de grandes quantités de données.</span><span class="sxs-lookup"><span data-stu-id="c32a3-115">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="c32a3-116">Les nouvelles fonctionnalités du langage dans C# 7.2 fournissent des mécanismes qui permettent d’obtenir un code efficace sécurisé en utilisant des références à des types valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-116">New language features in C# 7.2 provide mechanisms that enable safe efficient code using references to value types.</span></span> <span data-ttu-id="c32a3-117">Utilisez ces fonctionnalités judicieusement pour minimiser à la fois les allocations et les opérations de copie.</span><span class="sxs-lookup"><span data-stu-id="c32a3-117">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="c32a3-118">Cet article explore ces nouvelles fonctionnalités.</span><span class="sxs-lookup"><span data-stu-id="c32a3-118">This article explores those new features.</span></span>

<span data-ttu-id="c32a3-119">Cet article met l’accent sur les techniques de gestion des ressources suivantes :</span><span class="sxs-lookup"><span data-stu-id="c32a3-119">This article focuses on the following resource management techniques:</span></span>

- <span data-ttu-id="c32a3-120">Déclarez un [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) pour exprimer qu’un type est **immuable** et qu’il permet au compilateur d’enregistrer des copies lors de l’utilisation de paramètres [`in`](language-reference/keywords/in-parameter-modifier.md).</span><span class="sxs-lookup"><span data-stu-id="c32a3-120">Declare a [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) to express that a type is **immutable** and enables the compiler to save copies when using [`in`](language-reference/keywords/in-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="c32a3-121">Utilisez un retour de [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) quand la valeur de retour est un `struct` plus grand que <xref:System.IntPtr.Size?displayProperty=nameWithType> et que la durée de vie du stockage est supérieure à celle de la méthode retournant la valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-121">Use a [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) return when the return value is a `struct` larger than <xref:System.IntPtr.Size?displayProperty=nameWithType> and the storage lifetime is greater than the method returning the value.</span></span>
- <span data-ttu-id="c32a3-122">Quand la taille d’une `readonly struct` plus grande que <xref:System.IntPtr.Size?displayProperty=nameWithType>, vous devez la passer comme paramètre `in` pour des raisons de performances.</span><span class="sxs-lookup"><span data-stu-id="c32a3-122">When the size of a `readonly struct` is bigger than <xref:System.IntPtr.Size?displayProperty=nameWithType>, you should pass it as an `in` parameter for performance reasons.</span></span>
- <span data-ttu-id="c32a3-123">Ne passez jamais un `struct` comme paramètre `in`, sauf s’il est déclaré avec le modificateur `readonly`, car il peut affecter négativement les performances et conduire à un comportement obscur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-123">Never pass a `struct` as an `in` parameter unless it's declared with the `readonly` modifier because it may negatively affect performance and could lead to an obscure behavior.</span></span>
- <span data-ttu-id="c32a3-124">Utilisez un [ `ref struct` ](language-reference/keywords/ref.md#ref-struct-types), ou un `readonly ref struct` comme <xref:System.Span%601> ou <xref:System.ReadOnlySpan%601> pour utiliser la mémoire comme une séquence d’octets.</span><span class="sxs-lookup"><span data-stu-id="c32a3-124">Use a [`ref struct`](language-reference/keywords/ref.md#ref-struct-types), or a `readonly ref struct` such as <xref:System.Span%601> or <xref:System.ReadOnlySpan%601> to work with memory as a sequence of bytes.</span></span>

<span data-ttu-id="c32a3-125">Ces techniques vous forcent à trouver un équilibre entre deux objectifs concurrents concernant les **références** et les **valeurs**.</span><span class="sxs-lookup"><span data-stu-id="c32a3-125">These techniques force you to balance two competing goals with regard to **references** and **values**.</span></span> <span data-ttu-id="c32a3-126">Les variables qui sont des [types référence](programming-guide/types/index.md#reference-types) contiennent une référence à l’emplacement en mémoire.</span><span class="sxs-lookup"><span data-stu-id="c32a3-126">Variables that are [reference types](programming-guide/types/index.md#reference-types) hold a reference to the location in memory.</span></span> <span data-ttu-id="c32a3-127">Les variables qui sont des [types valeur](programming-guide/types/index.md#value-types) contiennent directement leur valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-127">Variables that are [value types](programming-guide/types/index.md#value-types) directly contain their value.</span></span> <span data-ttu-id="c32a3-128">Ces différences mettent en évidence les différences importantes pour la gestion des ressources mémoire.</span><span class="sxs-lookup"><span data-stu-id="c32a3-128">These differences highlight the key differences that are important for managing memory resources.</span></span> <span data-ttu-id="c32a3-129">Les **types valeur** sont généralement copiés quand ils sont passés à une méthode ou retournés depuis une méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-129">**Value types** are typically copied when passed to a method or returned from a method.</span></span> <span data-ttu-id="c32a3-130">Ce comportement comprend la copie de la valeur de `this` lors de l’appel de membres d’un type valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-130">This behavior includes copying the value of `this` when calling members of a value type.</span></span> <span data-ttu-id="c32a3-131">Le coût de la copie est lié à la taille du type.</span><span class="sxs-lookup"><span data-stu-id="c32a3-131">The cost of the copy is related to the size of the type.</span></span> <span data-ttu-id="c32a3-132">Les **types référence** sont alloués sur le tas managé.</span><span class="sxs-lookup"><span data-stu-id="c32a3-132">**Reference types** are allocated on the managed heap.</span></span> <span data-ttu-id="c32a3-133">Chaque nouvel objet nécessite une nouvelle allocation et doit par conséquent être récupéré par la suite.</span><span class="sxs-lookup"><span data-stu-id="c32a3-133">Each new object requires a new allocation, and subsequently must be reclaimed.</span></span> <span data-ttu-id="c32a3-134">Ces deux opérations prennent du temps.</span><span class="sxs-lookup"><span data-stu-id="c32a3-134">Both these operations take time.</span></span> <span data-ttu-id="c32a3-135">La référence est copiée quand un type référence est passé comme argument à une méthode ou retourné depuis une méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-135">The reference is copied when a reference type is passed as an argument to a method or returned from a method.</span></span>

<span data-ttu-id="c32a3-136">Cet article utilise l’exemple de concept suivant d’une structure de points 3D pour expliquer ces recommandations :</span><span class="sxs-lookup"><span data-stu-id="c32a3-136">This article uses the following example concept of the 3D-point structure to explain these recommendations:</span></span>

```csharp
public struct Point3D
{
    public double X;
    public double Y;
    public double Z;
}
```

<span data-ttu-id="c32a3-137">Les différents exemples utilisent des implémentations différentes de ce concept.</span><span class="sxs-lookup"><span data-stu-id="c32a3-137">Different examples use different implementations of this concept.</span></span>

## <a name="declare-readonly-structs-for-immutable-value-types"></a><span data-ttu-id="c32a3-138">Déclarer des structs en lecture seule pour les types valeur immuables</span><span class="sxs-lookup"><span data-stu-id="c32a3-138">Declare readonly structs for immutable value types</span></span>

<span data-ttu-id="c32a3-139">La déclaration d’une `struct` avec le modificateur `readonly` indique au compilateur que votre intention est de créer un type immuable.</span><span class="sxs-lookup"><span data-stu-id="c32a3-139">Declaring a `struct` using the `readonly` modifier informs the compiler that your intent is to create an immutable type.</span></span> <span data-ttu-id="c32a3-140">Le compilateur applique cette décision de conception avec les règles suivantes :</span><span class="sxs-lookup"><span data-stu-id="c32a3-140">The compiler enforces that design decision with the following rules:</span></span>

- <span data-ttu-id="c32a3-141">Tous les champs membres doivent être `readonly`</span><span class="sxs-lookup"><span data-stu-id="c32a3-141">All field members must be `readonly`</span></span>
- <span data-ttu-id="c32a3-142">Toutes les propriétés doivent être en lecture seule, y compris les propriétés implémentées automatiquement.</span><span class="sxs-lookup"><span data-stu-id="c32a3-142">All properties must be read-only, including auto-implemented properties.</span></span>

<span data-ttu-id="c32a3-143">Ces deux règles sont suffisantes pour garantir qu’aucun membre d’une `readonly struct` ne modifie l’état de ce struct.</span><span class="sxs-lookup"><span data-stu-id="c32a3-143">These two rules are sufficient to ensure that no member of a `readonly struct` modifies the state of that struct.</span></span> <span data-ttu-id="c32a3-144">Le `struct` est immuable.</span><span class="sxs-lookup"><span data-stu-id="c32a3-144">The `struct` is immutable.</span></span> <span data-ttu-id="c32a3-145">La structure `Point3D` peut être définie comme struct immuable, comme le montre l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="c32a3-145">The `Point3D` structure could be defined as an immutable struct as shown in the following example:</span></span>

```csharp
readonly public struct ReadonlyPoint3D
{
    public ReadonlyPoint3D(double x, double y, double z)
    {
        this.X = x;
        this.Y = y;
        this.Z = z;
    }

    public double X { get; }
    public double Y { get; }
    public double Z { get; }
}
```

<span data-ttu-id="c32a3-146">Suivez cette recommandation quand l’intention de votre conception est de créer un type immuable.</span><span class="sxs-lookup"><span data-stu-id="c32a3-146">Follow this recommendation whenever your design intent is to create an immutable value type.</span></span> <span data-ttu-id="c32a3-147">Toute amélioration des performances est un avantage supplémentaire.</span><span class="sxs-lookup"><span data-stu-id="c32a3-147">Any performance improvements are an added benefit.</span></span> <span data-ttu-id="c32a3-148">Le `readonly struct` exprime clairement l’intention de votre conception.</span><span class="sxs-lookup"><span data-stu-id="c32a3-148">The `readonly struct` clearly expresses your design intent.</span></span>

## <a name="use-ref-readonly-return-statements-for-large-structures-when-possible"></a><span data-ttu-id="c32a3-149">Quand c’est possible, utilisez des instructions `ref readonly return` pour les grandes structures.</span><span class="sxs-lookup"><span data-stu-id="c32a3-149">Use `ref readonly return` statements for large structures when possible</span></span>

<span data-ttu-id="c32a3-150">Vous pouvez retourner des valeurs par référence quand la valeur retournée n’est pas locale à la méthode effectuant le retour.</span><span class="sxs-lookup"><span data-stu-id="c32a3-150">You can return values by reference when the value being returned isn't local to the returning method.</span></span> <span data-ttu-id="c32a3-151">Un retour par référence signifie que seule la référence est copiée, et non pas la structure.</span><span class="sxs-lookup"><span data-stu-id="c32a3-151">Returning by reference means that only the reference is copied, not the structure.</span></span> <span data-ttu-id="c32a3-152">Dans l’exemple suivant, la propriété `Origin` ne peut pas utiliser un retour `ref`, car la valeur retournée est une variable locale :</span><span class="sxs-lookup"><span data-stu-id="c32a3-152">In the following example, the `Origin` property can't use a `ref` return because the value being returned is a local variable:</span></span>

```csharp
public Point3D Origin => new Point3D(0,0,0);
```

<span data-ttu-id="c32a3-153">Cependant, la définition de propriété suivante peut être retournée par référence, car la valeur retournée est un membre statique :</span><span class="sxs-lookup"><span data-stu-id="c32a3-153">However, the following property definition can be returned by reference because the returned value is a static member:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    // Dangerous! returning a mutable reference to internal storage
    public ref Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="c32a3-154">Vous ne voulez pas que les appelants modifient la valeur l’origine : vous devez donc retourner la valeur par `readonly ref` :</span><span class="sxs-lookup"><span data-stu-id="c32a3-154">You don't want callers modifying the origin, so you should return the value by `readonly ref`:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    public static ref readonly Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="c32a3-155">Retourner `ref readonly` vous permet d’éviter de copier des structures plus grandes et de préserver l’immuabilité de vos membres de données internes.</span><span class="sxs-lookup"><span data-stu-id="c32a3-155">Returning `ref readonly` enables you to save copying larger structures and preserve the immutability of your internal data members.</span></span>

<span data-ttu-id="c32a3-156">À l’endroit de l’appel, les appelants font le choix d’utiliser la propriété `Origin` comme `readonly ref` ou comme valeur :</span><span class="sxs-lookup"><span data-stu-id="c32a3-156">At the call site, callers make the choice to use the `Origin` property as a `readonly ref` or as a value:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="c32a3-157">La première assignation dans le code précédent effectue une copie de la constante `Origin` et assigne cette copie.</span><span class="sxs-lookup"><span data-stu-id="c32a3-157">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="c32a3-158">La seconde assigne une référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-158">The second assigns a reference.</span></span> <span data-ttu-id="c32a3-159">Notez que le modificateur `readonly` doit faire partie de la déclaration de la variable.</span><span class="sxs-lookup"><span data-stu-id="c32a3-159">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="c32a3-160">La référence à laquelle il fait référence ne peut pas être modifiée.</span><span class="sxs-lookup"><span data-stu-id="c32a3-160">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="c32a3-161">Toute tentative de modification provoque une erreur de compilation.</span><span class="sxs-lookup"><span data-stu-id="c32a3-161">Attempts to do so result in a compile-time error.</span></span>

<span data-ttu-id="c32a3-162">Le modificateur `readonly` est nécessaire sur la déclaration de `originReference`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-162">The `readonly` modifier is required on the declaration of `originReference`.</span></span>

<span data-ttu-id="c32a3-163">Le compilateur fait en sorte que l’appelant ne puisse pas modifier la référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-163">The compiler enforces that the caller can't modify the reference.</span></span> <span data-ttu-id="c32a3-164">Toute tentative d’assignation directe de la valeur génère une erreur de compilation.</span><span class="sxs-lookup"><span data-stu-id="c32a3-164">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="c32a3-165">Cependant, le compilateur ne peut pas savoir si une méthode membre modifie l’état du struct.</span><span class="sxs-lookup"><span data-stu-id="c32a3-165">However, the compiler can't know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="c32a3-166">Pour garantir que l’objet n’est pas modifié, le compilateur crée une copie et appelle les références de membre en utilisant cette copie.</span><span class="sxs-lookup"><span data-stu-id="c32a3-166">To ensure that the object isn't modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="c32a3-167">Les modifications sont apportées à cette copie défensive.</span><span class="sxs-lookup"><span data-stu-id="c32a3-167">Any modifications are to that defensive copy.</span></span>

## <a name="apply-the-in-modifier-to-readonly-struct-parameters-larger-than-systemintptrsize"></a><span data-ttu-id="c32a3-168">Appliquer le modificateur `in` à des paramètres `readonly struct` plus grands que `System.IntPtr.Size`</span><span class="sxs-lookup"><span data-stu-id="c32a3-168">Apply the `in` modifier to `readonly struct` parameters larger than `System.IntPtr.Size`</span></span>

<span data-ttu-id="c32a3-169">Le mot clé `in` vient en complément des mots clés `ref` et `out` existants pour passer des arguments par référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-169">The `in` keyword complements the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="c32a3-170">Le mot clé `in` spécifie que l’argument doit être passé par référence, mais la méthode appelée ne modifie pas la valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-170">The `in` keyword specifies passing the argument by reference, but the called method doesn't modify the value.</span></span>

<span data-ttu-id="c32a3-171">Vous disposez ainsi d’un vocabulaire complet pour exprimer votre intention de conception.</span><span class="sxs-lookup"><span data-stu-id="c32a3-171">This addition provides a full vocabulary to express your design intent.</span></span>
<span data-ttu-id="c32a3-172">Les types valeur sont copiés s’ils sont passés à une méthode appelée et que vous ne spécifiez pas l’un des modificateurs suivants dans la signature de la méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-172">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="c32a3-173">Chacun de ces modificateurs spécifie qu’une variable est passée par référence, ce qui évite la copie.</span><span class="sxs-lookup"><span data-stu-id="c32a3-173">Each of these modifiers specifies that a variable is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="c32a3-174">Chaque modificateur exprime une intention différente :</span><span class="sxs-lookup"><span data-stu-id="c32a3-174">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="c32a3-175">`out`: Cette méthode définit la valeur de l’argument utilisé comme paramètre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-175">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="c32a3-176">`ref`: Cette méthode peut définir la valeur de l’argument utilisé comme paramètre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-176">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="c32a3-177">`in`: Cette méthode ne modifie pas la valeur de l’argument utilisé comme paramètre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-177">`in`: This method doesn't modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="c32a3-178">Ajoutez le modificateur `in` pour passer un argument par référence et déclarez que votre intention de conception est de passer des arguments par référence afin d’éviter toute copie inutile.</span><span class="sxs-lookup"><span data-stu-id="c32a3-178">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="c32a3-179">Vous n’avez pas l’intention de modifier l’objet utilisé comme argument.</span><span class="sxs-lookup"><span data-stu-id="c32a3-179">You don't intend to modify the object used as that argument.</span></span>

<span data-ttu-id="c32a3-180">Cette pratique améliore souvent les performances pour les types valeur en lecture seule qui sont plus grands que <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="c32a3-180">This practice often improves performance for readonly value types that are larger than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span> <span data-ttu-id="c32a3-181">Pour les types simples (les types `sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal`, `bool` et `enum`), les gains de performances potentiels sont minimes.</span><span class="sxs-lookup"><span data-stu-id="c32a3-181">For simple types (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` and `bool`, and `enum` types), any potential performance gains are minimal.</span></span> <span data-ttu-id="c32a3-182">En fait, les performances peuvent se dégrader quand le passage par référence est utilisé pour les types plus petits que <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="c32a3-182">In fact, performance may degrade by using pass-by-reference for types smaller than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="c32a3-183">Le code suivant montre un exemple de méthode qui calcule la distance entre deux points dans l’espace 3D.</span><span class="sxs-lookup"><span data-stu-id="c32a3-183">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="c32a3-184">Les arguments sont deux structures qui contiennent chacune trois valeurs de type double.</span><span class="sxs-lookup"><span data-stu-id="c32a3-184">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="c32a3-185">Un double représentant 8 octets, chaque argument représente 24 octets.</span><span class="sxs-lookup"><span data-stu-id="c32a3-185">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="c32a3-186">En spécifiant le modificateur `in`, vous passez une référence de 4 ou 8 octets à ces arguments, selon l’architecture de la machine.</span><span class="sxs-lookup"><span data-stu-id="c32a3-186">By specifying the `in` modifier, you pass a 4 byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="c32a3-187">La différence de taille est minime, mais elle peut s’accentuer si votre application appelle cette méthode dans une boucle dense avec de nombreuses valeurs différentes.</span><span class="sxs-lookup"><span data-stu-id="c32a3-187">The difference in size is small, but it adds up when your application calls this method in a tight loop using many different values.</span></span>

<span data-ttu-id="c32a3-188">Le modificateur `in` complète aussi `out` et `ref` d’autres façons.</span><span class="sxs-lookup"><span data-stu-id="c32a3-188">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="c32a3-189">Vous ne pouvez pas créer des surcharges de méthode qui diffèrent seulement par la présence de `in`, `out` ou `ref`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-189">You can't create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="c32a3-190">Ces nouvelles règles étendent le même comportement qui a toujours été défini pour les paramètres `out` et `ref`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-190">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span> <span data-ttu-id="c32a3-191">Comme les modificateurs `out` et `ref`, les types valeur ne sont pas convertis (boxed), car le modificateur `in` est appliqué.</span><span class="sxs-lookup"><span data-stu-id="c32a3-191">Like the `out` and `ref` modifiers, value types aren't boxed because the `in` modifier is applied.</span></span>

<span data-ttu-id="c32a3-192">Le modificateur `in` peut être appliqué à tout membre acceptant des paramètres : méthodes, délégués, expressions lambda, fonctions locales, indexeurs, opérateurs.</span><span class="sxs-lookup"><span data-stu-id="c32a3-192">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="c32a3-193">Une autre caractéristique des paramètres `in` est que vous pouvez utiliser des valeurs littérales ou des constantes pour l’argument d’un paramètre `in`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-193">Another feature of `in` parameters is that you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="c32a3-194">Par ailleurs, contrairement à un paramètre `ref` ou `out`, il est inutile d’appliquer le modificateur `in` au niveau du site d’appel.</span><span class="sxs-lookup"><span data-stu-id="c32a3-194">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="c32a3-195">Le code suivant donne deux exemples d’appel de la méthode `CalculateDistance`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-195">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="c32a3-196">Le premier utilise deux variables locales passées par référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-196">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="c32a3-197">La deuxième inclut une variable temporaire créée dans le cadre de l’appel de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-197">The second includes a temporary variable created as part of the method call.</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="c32a3-198">Le compilateur garantit la nature « en lecture seule » d’un argument `in` de plusieurs façons.</span><span class="sxs-lookup"><span data-stu-id="c32a3-198">There are several ways in which the compiler enforces the read-only nature of an `in` argument.</span></span>  <span data-ttu-id="c32a3-199">Premièrement, la méthode appelée ne peut rien assigner directement à un paramètre `in`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-199">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="c32a3-200">Elle ne peut rien assigner directement à un champ d’un paramètre `in` quand cette valeur est un type `struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-200">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="c32a3-201">De plus, vous ne pouvez pas passer un paramètre `in` à une méthode en utilisant le modificateur `ref` ou `out`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-201">In addition, you can't pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="c32a3-202">Ces règles s’appliquent à tout champ d’un paramètre `in`, dans la mesure où le champ est un type `struct` et le paramètre également un type `struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-202">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="c32a3-203">En fait, ces règles s’appliquent à plusieurs couches d’accès au membre, dans la mesure où les types situés à tous les niveaux d’accès au membre sont des `structs`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-203">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span>
<span data-ttu-id="c32a3-204">Le compilateur considère que les types `struct` passés en tant qu’arguments `in` et leurs membres `struct` sont des variables en lecture seule quand ils sont utilisés en tant qu’arguments pour d’autres méthodes.</span><span class="sxs-lookup"><span data-stu-id="c32a3-204">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="c32a3-205">L’utilisation de paramètres `in` peut éviter les coûts potentiels en termes de performances qui sont liés aux copies.</span><span class="sxs-lookup"><span data-stu-id="c32a3-205">The use of `in` parameters can avoid the potential performance costs of making copies.</span></span> <span data-ttu-id="c32a3-206">Elle ne change pas la sémantique des appels de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-206">It doesn't change the semantics of any method call.</span></span> <span data-ttu-id="c32a3-207">Ainsi, vous n’avez pas besoin de spécifier le modificateur `in` à l’endroit de l’appel.</span><span class="sxs-lookup"><span data-stu-id="c32a3-207">Therefore, you don't need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="c32a3-208">L’omission du modificateur `in` à l’endroit de l’appel indique au compilateur qu’il est autorisé à effectuer une copie de l’argument pour les raisons suivantes :</span><span class="sxs-lookup"><span data-stu-id="c32a3-208">Omitting the `in` modifier at the call site informs the compiler that it's allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="c32a3-209">Il existe une conversion implicite, mais pas une conversion d’identité du type de l’argument en type du paramètre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-209">There exists an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="c32a3-210">L’argument est une expression, mais ne dispose pas d’une variable de stockage connue.</span><span class="sxs-lookup"><span data-stu-id="c32a3-210">The argument is an expression but doesn't have a known storage variable.</span></span>
- <span data-ttu-id="c32a3-211">Il existe une surcharge qui diffère selon la présence ou l’absence de `in`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-211">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="c32a3-212">Dans ce cas, la surcharge par valeur convient mieux.</span><span class="sxs-lookup"><span data-stu-id="c32a3-212">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="c32a3-213">Ces règles s’avèrent utiles quand vous mettez à jour le code existant pour utiliser des arguments de référence en lecture seule.</span><span class="sxs-lookup"><span data-stu-id="c32a3-213">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="c32a3-214">À l’intérieur de la méthode appelée, vous pouvez appeler toute méthode d’instance qui utilise des paramètres par valeur.</span><span class="sxs-lookup"><span data-stu-id="c32a3-214">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="c32a3-215">Dans ces instances, une copie du paramètre `in` est créée.</span><span class="sxs-lookup"><span data-stu-id="c32a3-215">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="c32a3-216">Étant donné que le compilateur peut créer une variable temporaire pour un paramètre `in`, vous pouvez aussi spécifier des valeurs par défaut pour n’importe quel paramètre `in`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-216">Because the compiler may create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="c32a3-217">Le code suivant spécifie l’origine (point 0,0) comme valeur par défaut du deuxième point :</span><span class="sxs-lookup"><span data-stu-id="c32a3-217">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="c32a3-218">Pour forcer le compilateur à passer des arguments en lecture seule par référence, spécifiez le modificateur `in` sur les arguments à l’endroit de l’appel, comme indiqué dans le code suivant :</span><span class="sxs-lookup"><span data-stu-id="c32a3-218">To force the compiler to pass read-only arguments by reference, specify the `in` modifier on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="c32a3-219">Ce comportement facilite l’adoption de paramètres `in` au fil du temps dans les codes bases volumineux où des gains de performance sont possibles.</span><span class="sxs-lookup"><span data-stu-id="c32a3-219">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="c32a3-220">Vous commencez par ajouter le modificateur `in` aux signatures de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-220">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="c32a3-221">Ensuite, vous pouvez ajouter le modificateur `in` aux endroits des appels et créer des types `readonly struct` pour permettre au compilateur d’éviter de créer des copies défensives des paramètres `in` à d’autres emplacements.</span><span class="sxs-lookup"><span data-stu-id="c32a3-221">Then, you can add the `in` modifier at call sites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="c32a3-222">La désignation du paramètre `in` peut également être utilisée avec des types référence ou des valeurs numériques.</span><span class="sxs-lookup"><span data-stu-id="c32a3-222">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="c32a3-223">Toutefois, les avantages dans les deux cas sont minimes, voire inexistants.</span><span class="sxs-lookup"><span data-stu-id="c32a3-223">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="never-use-mutable-structs-as-in-in-argument"></a><span data-ttu-id="c32a3-224">N’utilisez jamais des structs mutables comme argument `in`</span><span class="sxs-lookup"><span data-stu-id="c32a3-224">Never use mutable structs as in `in` argument</span></span>

<span data-ttu-id="c32a3-225">Les techniques décrites ci-dessus expliquent comment éviter les copies en retournant des références et en passant les valeurs par référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-225">The techniques described above explain how to avoid copies by returning references and passing values by reference.</span></span> <span data-ttu-id="c32a3-226">Ces techniques conviennent le mieux quand les types d’arguments sont déclarés comme types `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-226">These techniques work best when the argument types are declared as `readonly struct` types.</span></span> <span data-ttu-id="c32a3-227">Sinon, le compilateur doit créer des **copies défensives** dans de nombreuses situations pour garantir la nature « en lecture seule » des arguments.</span><span class="sxs-lookup"><span data-stu-id="c32a3-227">Otherwise, the compiler must create **defensive copies** in many situations to enforce the readonly-ness of any arguments.</span></span> <span data-ttu-id="c32a3-228">Considérons l’exemple suivant, qui calcule la distance d’un point 3D depuis l’origine :</span><span class="sxs-lookup"><span data-stu-id="c32a3-228">Consider the following example that calculates the distance of a 3D point from the origin:</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="c32a3-229">La structure `Point3D` n’est *pas* un struct en lecture seule.</span><span class="sxs-lookup"><span data-stu-id="c32a3-229">The `Point3D` structure is *not* a readonly struct.</span></span> <span data-ttu-id="c32a3-230">Il y a six appels différents d’accès aux propriétés dans le corps de cette méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-230">There are six different property access calls in the body of this method.</span></span> <span data-ttu-id="c32a3-231">En première approche, vous pouvez penser que ces accès sont sécurisés.</span><span class="sxs-lookup"><span data-stu-id="c32a3-231">On first examination, you may have thought these accesses were safe.</span></span> <span data-ttu-id="c32a3-232">Après tout, un accesseur `get` ne devrait pas modifier l’état de l’objet.</span><span class="sxs-lookup"><span data-stu-id="c32a3-232">After all, a `get` accessor shouldn't modify the state of the object.</span></span> <span data-ttu-id="c32a3-233">Mais il n’existe aucune règle du langage qui le garantisse.</span><span class="sxs-lookup"><span data-stu-id="c32a3-233">But there's no language rule that enforces that.</span></span> <span data-ttu-id="c32a3-234">Il s’agit seulement d’une convention courante.</span><span class="sxs-lookup"><span data-stu-id="c32a3-234">It's only a common convention.</span></span> <span data-ttu-id="c32a3-235">N’importe quel type peut implémenter un accesseur `get` qui a modifié l’état interne.</span><span class="sxs-lookup"><span data-stu-id="c32a3-235">Any type could implement a `get` accessor that modified the internal state.</span></span> <span data-ttu-id="c32a3-236">Sans garantie fournie par le langage, le compilateur doit créer une copie temporaire de l’argument avant d’appeler un membre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-236">Without some language guarantee, the compiler must create a temporary copy of the argument before calling any member.</span></span> <span data-ttu-id="c32a3-237">Le stockage temporaire est créé sur la pile, les valeurs de l’argument sont copiées dans le stockage temporaire et la valeur est copiée sur la pile pour chaque accès au membre en tant qu’argument `this`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-237">The temporary storage is created on the stack, the values of the argument are copied to the temporary storage, and the value is copied to the stack for each member access as the `this` argument.</span></span> <span data-ttu-id="c32a3-238">Dans de nombreux cas, ces copies nuisent suffisamment aux performances pour que ce passage par valeur soit plus rapide qu’un passage par référence en lecture seule quand le type d’argument n’est pas un `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-238">In many situations, these copies harm performance enough that pass-by-value is faster than pass-by-readonly-reference when the argument type isn't a `readonly struct`.</span></span>

<span data-ttu-id="c32a3-239">Au lieu de cela, si le calcul de distance utilise le struct immuable, `ReadonlyPoint3D`, des objets temporaires ne sont pas nécessaires :</span><span class="sxs-lookup"><span data-stu-id="c32a3-239">Instead, if the distance calculation uses the immutable struct, `ReadonlyPoint3D`, temporary objects are not needed:</span></span>

[!code-csharp[readonlyInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ReadOnlyInArgument "Specifying a readonly in argument")]

<span data-ttu-id="c32a3-240">Le compilateur génère un code plus efficace quand vous appelez des membres d’un `readonly struct` : La référence `this`, au lieu d’une copie du récepteur, est toujours un paramètre `in` passé par référence à la méthode de membre.</span><span class="sxs-lookup"><span data-stu-id="c32a3-240">The compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="c32a3-241">Cette optimisation évite la copie quand vous utilisez un `readonly struct` comme argument `in`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-241">This optimization saves copying when you use a `readonly struct` as an `in` argument.</span></span>

<span data-ttu-id="c32a3-242">Vous pouvez voir un exemple de programme qui montre les différences de performances avec [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) dans notre [dépôt d’exemples](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) sur GitHub.</span><span class="sxs-lookup"><span data-stu-id="c32a3-242">You can see an example program that demonstrates the performance differences using [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) in our [samples repository](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) on GitHub.</span></span> <span data-ttu-id="c32a3-243">Il compare le passage d’un struct mutable par valeur et par référence au passage d’un struct immuable par valeur et par référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-243">It compares passing a mutable struct by value and by reference with passing an immutable struct by value and by reference.</span></span> <span data-ttu-id="c32a3-244">L’utilisation du struct immuable et le passage par référence est le plus rapide.</span><span class="sxs-lookup"><span data-stu-id="c32a3-244">The use of the immutable struct and pass by reference is fastest.</span></span>

## <a name="use-ref-struct-types-to-work-with-blocks-or-memory-on-a-single-stack-frame"></a><span data-ttu-id="c32a3-245">Utilisez des types `ref struct` pour travailler avec des blocs ou de la mémoire sur un frame de pile</span><span class="sxs-lookup"><span data-stu-id="c32a3-245">Use `ref struct` types to work with blocks or memory on a single stack frame</span></span>

<span data-ttu-id="c32a3-246">Une autre fonctionnalité associée du langage est la possibilité de déclarer un type valeur qui doit être contraint à un seul frame de pile.</span><span class="sxs-lookup"><span data-stu-id="c32a3-246">A related language feature is the ability to declare a value type that must be constrained to a single stack frame.</span></span> <span data-ttu-id="c32a3-247">Cette restriction permet au compilateur d’effectuer plusieurs optimisations.</span><span class="sxs-lookup"><span data-stu-id="c32a3-247">This restriction enables the compiler to make several optimizations.</span></span> <span data-ttu-id="c32a3-248">Cette fonctionnalité vise principalement <xref:System.Span%601> et ses structures associées.</span><span class="sxs-lookup"><span data-stu-id="c32a3-248">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="c32a3-249">Vous obtiendrez de meilleures performances de ces améliorations en utilisant les API .NET nouvelles et mises à jour, qui facilitent l’utilisation du type <xref:System.Span%601>.</span><span class="sxs-lookup"><span data-stu-id="c32a3-249">You'll achieve performance improvements from these enhancements by using new and updated .NET APIs that make use of the <xref:System.Span%601> type.</span></span>

<span data-ttu-id="c32a3-250">Vous pouvez avoir des exigences similaires quand vous utilisez de la mémoire créée avec [`stackalloc`](language-reference/keywords/stackalloc.md) ou de la mémoire provenant d’API d’interopérabilité.</span><span class="sxs-lookup"><span data-stu-id="c32a3-250">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="c32a3-251">Pour répondre à ces besoins, vous pouvez définir vos propres types `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-251">You can define your own `ref struct` types for those needs.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="c32a3-252">Type `readonly ref struct`</span><span class="sxs-lookup"><span data-stu-id="c32a3-252">`readonly ref struct` type</span></span>

<span data-ttu-id="c32a3-253">La déclaration d’un struct comme `readonly ref` combine les avantages et les restrictions des déclarations `ref struct` et `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="c32a3-253">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> <span data-ttu-id="c32a3-254">La mémoire utilisée par l’étendue en lecture seule est limitée à un seul frame de pile, et cette mémoire ne peut pas être modifiée.</span><span class="sxs-lookup"><span data-stu-id="c32a3-254">The memory used by the readonly span is restricted to a single stack frame, and the memory used by the readonly span can't be modified.</span></span>

## <a name="conclusions"></a><span data-ttu-id="c32a3-255">Conclusions</span><span class="sxs-lookup"><span data-stu-id="c32a3-255">Conclusions</span></span>

<span data-ttu-id="c32a3-256">L’utilisation de types valeur réduit le nombre d’opérations d’allocation :</span><span class="sxs-lookup"><span data-stu-id="c32a3-256">Using value types minimizes the number of allocation operations:</span></span>

- <span data-ttu-id="c32a3-257">Le stockage pour les types valeur est alloué sur la pile les variables locales et les arguments de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-257">Storage for value types is stack allocated for local variables and method arguments.</span></span>
- <span data-ttu-id="c32a3-258">Le stockage pour les types valeur qui sont membres d’autres objets est alloué dans le cadre de cet objet, et non pas comme allocation distincte.</span><span class="sxs-lookup"><span data-stu-id="c32a3-258">Storage for value types that are members of other objects is allocated as part of that object, not as a separate allocation.</span></span>
- <span data-ttu-id="c32a3-259">Le stockage pour les valeurs de retour de type valeur est alloué sur la pile.</span><span class="sxs-lookup"><span data-stu-id="c32a3-259">Storage for value type return values is stack allocated.</span></span>

<span data-ttu-id="c32a3-260">Comparez cela avec les types référence dans ces mêmes situations :</span><span class="sxs-lookup"><span data-stu-id="c32a3-260">Contrast that with reference types in those same situations:</span></span>

- <span data-ttu-id="c32a3-261">Le stockage pour les types référence est alloué sur le tas pour les variables locales et les arguments de méthode.</span><span class="sxs-lookup"><span data-stu-id="c32a3-261">Storage for reference types are heap allocated for local variables and method arguments.</span></span> <span data-ttu-id="c32a3-262">La référence est stockée sur la pile.</span><span class="sxs-lookup"><span data-stu-id="c32a3-262">The reference is stored on the stack.</span></span>
- <span data-ttu-id="c32a3-263">Le stockage pour les types référence qui sont membres d’autres objets sont alloués séparément sur le tas.</span><span class="sxs-lookup"><span data-stu-id="c32a3-263">Storage for reference types that are members of other objects are separately allocated on the heap.</span></span> <span data-ttu-id="c32a3-264">L’objet conteneur stocke la référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-264">The containing object stores the reference.</span></span>
- <span data-ttu-id="c32a3-265">Le stockage des valeurs de retour de type référence est alloué sur le tas.</span><span class="sxs-lookup"><span data-stu-id="c32a3-265">Storage for reference type return values is heap allocated.</span></span> <span data-ttu-id="c32a3-266">La référence à ce stockage est stockée sur la pile.</span><span class="sxs-lookup"><span data-stu-id="c32a3-266">The reference to that storage is stored on the stack.</span></span>

<span data-ttu-id="c32a3-267">La réduction des allocations entraîne des compromis.</span><span class="sxs-lookup"><span data-stu-id="c32a3-267">Minimizing allocations comes with tradeoffs.</span></span> <span data-ttu-id="c32a3-268">Vous copiez plus de mémoire quand la taille du `struct` est supérieure à la taille d’une référence.</span><span class="sxs-lookup"><span data-stu-id="c32a3-268">You copy more memory when the size of the `struct` is larger than the size of a reference.</span></span> <span data-ttu-id="c32a3-269">Une référence est généralement sur 64 ou 32 bits, et elle varie en fonction de l’UC de la machine cible.</span><span class="sxs-lookup"><span data-stu-id="c32a3-269">A reference is typically 64 bits or 32 bits, and depends on the target machine CPU.</span></span>

<span data-ttu-id="c32a3-270">Ces compromis ont généralement un impact minimal sur les performances.</span><span class="sxs-lookup"><span data-stu-id="c32a3-270">These tradeoffs generally have minimal performance impact.</span></span> <span data-ttu-id="c32a3-271">Cependant, pour les grands structs ou des collections plus grandes, l’impact sur les performances augmente.</span><span class="sxs-lookup"><span data-stu-id="c32a3-271">However, for large structs or larger collections, the performance impact increases.</span></span> <span data-ttu-id="c32a3-272">L’impact peut être important dans des boucles denses et des chemins d’exécution intensifs pour les programmes.</span><span class="sxs-lookup"><span data-stu-id="c32a3-272">The impact can be large in tight loops and hot paths for programs.</span></span>

<span data-ttu-id="c32a3-273">Ces améliorations apportées au langage C# sont conçues pour les algorithmes dont les performances sont critiques, dans lesquels la réduction des allocations mémoire est un facteur essentiel pour atteindre le niveau de performance nécessaire.</span><span class="sxs-lookup"><span data-stu-id="c32a3-273">These enhancements to the C# language are designed for performance critical algorithms where minimizing memory allocations is a major factor in achieving the necessary performance.</span></span> <span data-ttu-id="c32a3-274">Vous constaterez peut-être que vous n’utilisez pas souvent ces fonctionnalités dans votre code.</span><span class="sxs-lookup"><span data-stu-id="c32a3-274">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="c32a3-275">Cependant, ces améliorations ont été adoptées dans .NET.</span><span class="sxs-lookup"><span data-stu-id="c32a3-275">However, these enhancements have been adopted throughout .NET.</span></span> <span data-ttu-id="c32a3-276">Comme de plus en plus d’API adoptent ces fonctionnalités, vous verrez les performances de vos applications s’améliorer.</span><span class="sxs-lookup"><span data-stu-id="c32a3-276">As more and more APIs make use of these features, you'll see the performance of your applications improve.</span></span>

## <a name="see-also"></a><span data-ttu-id="c32a3-277">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="c32a3-277">See also</span></span>

- [<span data-ttu-id="c32a3-278">ref, mot clé</span><span class="sxs-lookup"><span data-stu-id="c32a3-278">ref keyword</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="c32a3-279">Retours ref et variables locales ref</span><span class="sxs-lookup"><span data-stu-id="c32a3-279">Ref returns and ref locals</span></span>](programming-guide/classes-and-structs/ref-returns.md)
