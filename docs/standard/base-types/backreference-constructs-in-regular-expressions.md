---
title: Constructions de référence arrière dans les expressions régulières .NET
description: Découvrez comment identifier les éléments textuels répétés avec des constructions de référence arrière dans une expression régulière.
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- backreferences
- constructs, backreference
- .NET Framework regular expressions, backreference constructs
- regular expressions, backreference constructs
ms.assetid: 567a4b8d-0e79-49dc-8df9-f4b1aa376a2a
author: rpetrusha
ms.author: ronpet
ms.custom: seodec18
ms.openlocfilehash: d478ae9e1db86718236da73917d772820707ea03
ms.sourcegitcommit: 58fc0e6564a37fa1b9b1b140a637e864c4cf696e
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/08/2019
ms.locfileid: "57678356"
---
# <a name="backreference-constructs-in-regular-expressions"></a><span data-ttu-id="b0eb6-103">Constructions de backreference dans les expressions régulières</span><span class="sxs-lookup"><span data-stu-id="b0eb6-103">Backreference Constructs in Regular Expressions</span></span>

<span data-ttu-id="b0eb6-104">Les références arrière offrent un moyen pratique d’identifier un caractère répété ou une sous-chaîne répétée dans une chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-104">Backreferences provide a convenient way to identify a repeated character or substring within a string.</span></span> <span data-ttu-id="b0eb6-105">Par exemple, si la chaîne d’entrée contient plusieurs occurrences d’une sous-chaîne arbitraire, vous pouvez faire correspondre la première occurrence à un groupe de capture, puis utiliser une référence arrière pour faire correspondre les occurrences suivantes de la sous-chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-105">For example, if the input string contains multiple occurrences of an arbitrary substring, you can match the first occurrence with a capturing group, and then use a backreference to match subsequent occurrences of the substring.</span></span>

> [!NOTE]
> <span data-ttu-id="b0eb6-106">Une syntaxe distincte est utilisée pour faire référence à des groupes de capture nommés et numérotés dans les chaînes de remplacement.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-106">A separate syntax is used to refer to named and numbered capturing groups in replacement strings.</span></span> <span data-ttu-id="b0eb6-107">Pour plus d'informations, consultez [Substitutions](substitutions-in-regular-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="b0eb6-107">For more information, see [Substitutions](substitutions-in-regular-expressions.md).</span></span>

<span data-ttu-id="b0eb6-108">.NET définit des éléments de langage différents pour faire référence aux groupes de capture nommés et numérotés.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-108">.NET defines separate language elements to refer to numbered and named capturing groups.</span></span> <span data-ttu-id="b0eb6-109">Pour plus d’informations sur les groupes de capture, consultez [Constructions de regroupement](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="b0eb6-109">For more information about capturing groups, see [Grouping Constructs](../../../docs/standard/base-types/grouping-constructs-in-regular-expressions.md).</span></span>

## <a name="numbered-backreferences"></a><span data-ttu-id="b0eb6-110">Références arrière numérotées</span><span class="sxs-lookup"><span data-stu-id="b0eb6-110">Numbered Backreferences</span></span>

<span data-ttu-id="b0eb6-111">Une référence arrière numérotée utilise la syntaxe suivante :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-111">A numbered backreference uses the following syntax:</span></span>

<span data-ttu-id="b0eb6-112">`\` *nombre*</span><span class="sxs-lookup"><span data-stu-id="b0eb6-112">`\` *number*</span></span>

<span data-ttu-id="b0eb6-113">où *numéro* est la position ordinale du groupe de capture dans l’expression régulière.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-113">where *number* is the ordinal position of the capturing group in the regular expression.</span></span> <span data-ttu-id="b0eb6-114">Par exemple, `\4` correspond au contenu du quatrième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-114">For example, `\4` matches the contents of the fourth capturing group.</span></span> <span data-ttu-id="b0eb6-115">Si *numéro* n’est pas défini dans le modèle d’expression régulière, une erreur d’analyse se produit et le moteur d’expression régulière lève une exception <xref:System.ArgumentException>.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-115">If *number* is not defined in the regular expression pattern, a parsing error occurs, and the regular expression engine throws an <xref:System.ArgumentException>.</span></span> <span data-ttu-id="b0eb6-116">Par exemple, l’expression régulière `\b(\w+)\s\1` est valide, car `(\w+)` est le premier et unique groupe de capture dans l’expression.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-116">For example, the regular expression `\b(\w+)\s\1` is valid, because `(\w+)` is the first and only capturing group in the expression.</span></span> <span data-ttu-id="b0eb6-117">D’un autre côté, `\b(\w+)\s\2` n’est pas valide et lève une exception d’argument, car il n’existe aucun groupe de capture numéroté `\2`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-117">On the other hand, `\b(\w+)\s\2` is invalid and throws an argument exception, because there is no capturing group numbered `\2`.</span></span> <span data-ttu-id="b0eb6-118">En outre, si *nombre* identifie un groupe de capture dans une position ordinale particulière, mais qu’un nom numérique différent de sa position ordinale a été affecté au groupe de capture, l’analyseur d’expression régulière lève également une <xref:System.ArgumentException>.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-118">In addition, if *number* identifies a capturing group in a particular ordinal position, but that capturing group has been assigned a numeric name different than its ordinal position, the regular expression parser also throws an <xref:System.ArgumentException>.</span></span>

<span data-ttu-id="b0eb6-119">Remarquez l’ambiguïté entre les codes d’échappement octaux (tels que `\16`) et les références arrière `\`*numéro* qui utilisent la même notation.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-119">Note the ambiguity between octal escape codes (such as `\16`) and `\`*number* backreferences that use the same notation.</span></span> <span data-ttu-id="b0eb6-120">Cette ambiguïté est résolue comme suit :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-120">This ambiguity is resolved as follows:</span></span>

- <span data-ttu-id="b0eb6-121">Les expressions `\1` à `\9` sont toujours interprétées comme références arrière et non comme codes octaux.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-121">The expressions `\1` through `\9` are always interpreted as backreferences, and not as octal codes.</span></span>

- <span data-ttu-id="b0eb6-122">Si le premier chiffre d’une expression à plusieurs chiffres est 8 ou 9 (comme `\80` ou `\91`), l’expression est interprétée comme littéral.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-122">If the first digit of a multidigit expression is 8 or 9 (such as `\80` or `\91`), the expression as interpreted as a literal.</span></span>

- <span data-ttu-id="b0eb6-123">Les expressions à partir de `\10` et plus sont considérées comme des références arrière s’il existe une référence arrière correspondant à ce numéro ; sinon, elles sont interprétées comme des codes octaux.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-123">Expressions from `\10` and greater are considered backreferences if there is a backreference corresponding to that number; otherwise, they are interpreted as octal codes.</span></span>

- <span data-ttu-id="b0eb6-124">Si une expression régulière contient une référence arrière à un numéro de groupe non défini, une erreur d’analyse se produit et le moteur d’expression régulière lève une exception <xref:System.ArgumentException>.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-124">If a regular expression contains a backreference to an undefined group number, a parsing error occurs, and the regular expression engine throws an <xref:System.ArgumentException>.</span></span>

<span data-ttu-id="b0eb6-125">Si l’ambiguïté est un problème, vous pouvez utiliser la notation `\k<`*nom*`>`, qui est sans équivoque et ne peut pas être confondue avec les codes de caractères octaux.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-125">If the ambiguity is a problem, you can use the `\k<`*name*`>` notation, which is unambiguous and cannot be confused with octal character codes.</span></span> <span data-ttu-id="b0eb6-126">De même, les codes hexadécimaux tels que `\xdd` ne sont pas ambigus et ne peuvent pas être confondus avec les références arrière.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-126">Similarly, hexadecimal codes such as `\xdd` are unambiguous and cannot be confused with backreferences.</span></span>

<span data-ttu-id="b0eb6-127">L’exemple suivant recherche des caractères de mot doubles dans une chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-127">The following example finds doubled word characters in a string.</span></span> <span data-ttu-id="b0eb6-128">Il définit une expression régulière, `(\w)\1`, qui se compose des éléments suivants.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-128">It defines a regular expression, `(\w)\1`, which consists of the following elements.</span></span>

|<span data-ttu-id="b0eb6-129">Élément</span><span class="sxs-lookup"><span data-stu-id="b0eb6-129">Element</span></span>|<span data-ttu-id="b0eb6-130">Description</span><span class="sxs-lookup"><span data-stu-id="b0eb6-130">Description</span></span>|
|-------------|-----------------|
|`(\w)`|<span data-ttu-id="b0eb6-131">Mettre en correspondance un caractère de mot et l’affecter au premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-131">Match a word character and assign it to the first capturing group.</span></span>|
|`\1`|<span data-ttu-id="b0eb6-132">Mettre en correspondance le caractère suivant dont la valeur est identique à celle du premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-132">Match the next character that is the same as the value of the first capturing group.</span></span>|

[!code-csharp[RegularExpressions.Language.Backreferences#1](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference1.cs#1)]
[!code-vb[RegularExpressions.Language.Backreferences#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference1.vb#1)]

## <a name="named-backreferences"></a><span data-ttu-id="b0eb6-133">Références arrière nommées</span><span class="sxs-lookup"><span data-stu-id="b0eb6-133">Named Backreferences</span></span>

<span data-ttu-id="b0eb6-134">Une référence arrière nommée est définie avec la syntaxe suivante :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-134">A named backreference is defined by using the following syntax:</span></span>

<span data-ttu-id="b0eb6-135">`\k<` *name* `>`</span><span class="sxs-lookup"><span data-stu-id="b0eb6-135">`\k<` *name* `>`</span></span>

<span data-ttu-id="b0eb6-136">ou :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-136">or:</span></span>

<span data-ttu-id="b0eb6-137">`\k'` *name* `'`</span><span class="sxs-lookup"><span data-stu-id="b0eb6-137">`\k'` *name* `'`</span></span>

<span data-ttu-id="b0eb6-138">où *nom* est le nom d’un groupe de capture défini dans le modèle d’expression régulière.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-138">where *name* is the name of a capturing group defined in the regular expression pattern.</span></span> <span data-ttu-id="b0eb6-139">Si *nom* n’est pas défini dans le modèle d’expression régulière, une erreur d’analyse se produit et le moteur d’expression régulière lève une exception <xref:System.ArgumentException>.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-139">If *name* is not defined in the regular expression pattern, a parsing error occurs, and the regular expression engine throws an <xref:System.ArgumentException>.</span></span>

<span data-ttu-id="b0eb6-140">L’exemple suivant recherche des caractères de mot doubles dans une chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-140">The following example finds doubled word characters in a string.</span></span> <span data-ttu-id="b0eb6-141">Il définit une expression régulière, `(?<char>\w)\k<char>`, qui se compose des éléments suivants.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-141">It defines a regular expression, `(?<char>\w)\k<char>`, which consists of the following elements.</span></span>

|<span data-ttu-id="b0eb6-142">Élément</span><span class="sxs-lookup"><span data-stu-id="b0eb6-142">Element</span></span>|<span data-ttu-id="b0eb6-143">Description</span><span class="sxs-lookup"><span data-stu-id="b0eb6-143">Description</span></span>|
|-------------|-----------------|
|`(?<char>\w)`|<span data-ttu-id="b0eb6-144">Mettre en correspondance un caractère de mot et l’affecter à un groupe de capture nommé `char`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-144">Match a word character and assign it to a capturing group named `char`.</span></span>|
|`\k<char>`|<span data-ttu-id="b0eb6-145">Mettre en correspondance le caractère suivant dont la valeur est identique à celle du groupe de capture `char`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-145">Match the next character that is the same as the value of the `char` capturing group.</span></span>|

[!code-csharp[RegularExpressions.Language.Backreferences#2](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference2.cs#2)]
[!code-vb[RegularExpressions.Language.Backreferences#2](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference2.vb#2)]

## <a name="named-numeric-backreferences"></a><span data-ttu-id="b0eb6-146">Références arrières numériques nommées</span><span class="sxs-lookup"><span data-stu-id="b0eb6-146">Named numeric backreferences</span></span>

<span data-ttu-id="b0eb6-147">Dans une référence arrière nommée avec `\k`, *nom* peut également être la représentation d’un nombre sous forme de chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-147">In a named backreference with `\k`, *name* can also be the string representation of a number.</span></span> <span data-ttu-id="b0eb6-148">Par exemple, l’exemple suivant utilise l’expression régulière `(?<2>\w)\k<2>` pour rechercher des caractères de mot doubles dans une chaîne.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-148">For example, the following example uses the regular expression `(?<2>\w)\k<2>` to find doubled word characters in a string.</span></span> <span data-ttu-id="b0eb6-149">Dans ce cas, l’exemple définit un groupe de capture explicitement nommé « 2 », et la référence arrière est nommée en conséquence « 2 ».</span><span class="sxs-lookup"><span data-stu-id="b0eb6-149">In this case, the example defines a capturing group that is explicitly named "2", and the backreference is correspondingly named "2".</span></span>

[!code-csharp[RegularExpressions.Language.Backreferences#3](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference3.cs#3)]
[!code-vb[RegularExpressions.Language.Backreferences#3](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference3.vb#3)]

<span data-ttu-id="b0eb6-150">Si *nom* est la représentation d’un nombre sous forme de chaîne et qu’aucun groupe de capture n’a ce nom, `\k<`*nom* `>` est identique au `\` *nombre* de la référence arrière, où *nombre* est la position ordinale de la capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-150">If *name* is the string representation of a number, and no capturing group has that name, `\k<`*name*`>` is the same as the backreference `\`*number*, where *number* is the ordinal position of the capture.</span></span> <span data-ttu-id="b0eb6-151">Dans l’exemple suivant, il existe un seul groupe de capture nommé `char`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-151">In the following example, there is a single capturing group named `char`.</span></span> <span data-ttu-id="b0eb6-152">La construction de la référence arrière la référence en tant que `\k<1>`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-152">The backreference construct refers to it as `\k<1>`.</span></span> <span data-ttu-id="b0eb6-153">Comme la sortie de l’exemple le montre, l’appel à <xref:System.Text.RegularExpressions.Regex.IsMatch%2A?displayProperty=nameWithType> réussit, car `char` est le premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-153">As the output from the example shows, the call to the <xref:System.Text.RegularExpressions.Regex.IsMatch%2A?displayProperty=nameWithType> succeeds because `char` is the first capturing group.</span></span>

[!code-csharp[Ordinal.Backreference](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference6.cs)]
[!code-vb[Ordinal.BackReference](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference6.vb)]

<span data-ttu-id="b0eb6-154">Cependant, si *nom* est la représentation sous forme de chaîne d’un nombre et qu’un nom numérique a été affecté explicitement à ce groupe de capture dans cette position, l’analyseur d’expression régulière ne peut pas identifier le groupe de capture par sa position ordinale.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-154">However, if *name* is the string representation of a number and the capturing group in that position has been explicitly assigned a numeric name, the regular expression parser cannot identify the capturing group by its ordinal position.</span></span> <span data-ttu-id="b0eb6-155">Au lieu de cela, elle lève une <xref:System.ArgumentException>. Le seul groupe de capture dans l’exemple suivant est nommé « 2 ».</span><span class="sxs-lookup"><span data-stu-id="b0eb6-155">Instead, it throws an <xref:System.ArgumentException>.The only capturing group in the following example is named "2".</span></span> <span data-ttu-id="b0eb6-156">Comme la construction `\k` est utilisée pour définir une référence arrière nommée « 1 », l’analyseur d’expression régulière ne peut pas identifier le premier groupe de capture et lève une exception.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-156">Because the `\k` construct is used to define a backreference named "1", the regular expression parser is unable to identify the first capturing group and throws an exception.</span></span>

[!code-csharp[Ordinal.Backreference](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference7.cs)]
[!code-vb[Ordinal.BackReference](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference7.vb)]

## <a name="what-backreferences-match"></a><span data-ttu-id="b0eb6-157">Correspondance des références arrière</span><span class="sxs-lookup"><span data-stu-id="b0eb6-157">What Backreferences Match</span></span>

<span data-ttu-id="b0eb6-158">Une référence arrière référence la définition la plus récente d’un groupe (la définition la plus immédiatement à gauche, dans le cadre d’une mise en correspondance de gauche à droite).</span><span class="sxs-lookup"><span data-stu-id="b0eb6-158">A backreference refers to the most recent definition of a group (the definition most immediately to the left, when matching left to right).</span></span> <span data-ttu-id="b0eb6-159">Quand un groupe effectue plusieurs captures, une référence arrière référence la capture la plus récente.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-159">When a group makes multiple captures, a backreference refers to the most recent capture.</span></span>

<span data-ttu-id="b0eb6-160">L’exemple suivant inclut un modèle d’expression régulière, `(?<1>a)(?<1>\1b)*`, qui redéfinit le groupe nommé \1.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-160">The following example includes a regular expression pattern, `(?<1>a)(?<1>\1b)*`, which redefines the \1 named group.</span></span> <span data-ttu-id="b0eb6-161">Le tableau suivant décrit chaque modèle dans l’expression régulière.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-161">The following table describes each pattern in the regular expression.</span></span>

|<span data-ttu-id="b0eb6-162">Motif</span><span class="sxs-lookup"><span data-stu-id="b0eb6-162">Pattern</span></span>|<span data-ttu-id="b0eb6-163">Description</span><span class="sxs-lookup"><span data-stu-id="b0eb6-163">Description</span></span>|
|-------------|-----------------|
|`(?<1>a)`|<span data-ttu-id="b0eb6-164">Mettre en correspondance le caractère « a » et affecter le résultat au groupe de capture nommé `1`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-164">Match the character "a" and assign the result to the capturing group named `1`.</span></span>|
|`(?<1>\1b)*`|<span data-ttu-id="b0eb6-165">Mettre en correspondance zéro ou plus d’occurrences du groupe nommé `1` avec un « b » et affecter le résultat au groupe de capture nommé `1`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-165">Match zero or more occurrences of the group named `1` along with a "b", and assign the result to the capturing group named `1`.</span></span>|

[!code-csharp[RegularExpressions.Language.Backreferences#4](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference4.cs#4)]
[!code-vb[RegularExpressions.Language.Backreferences#4](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference4.vb#4)]

<span data-ttu-id="b0eb6-166">En comparant l’expression régulière à la chaîne d’entrée (« aababb »), le moteur d’expression régulière effectue les opérations suivantes :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-166">In comparing the regular expression with the input string ("aababb"), the regular expression engine performs the following operations:</span></span>

1. <span data-ttu-id="b0eb6-167">Il commence au début de la chaîne et réussit à mettre en correspondance « a » avec l’expression `(?<1>a)`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-167">It starts at the beginning of the string, and successfully matches "a" with the expression `(?<1>a)`.</span></span> <span data-ttu-id="b0eb6-168">La valeur du groupe `1` est maintenant « a ».</span><span class="sxs-lookup"><span data-stu-id="b0eb6-168">The value of the `1` group is now "a".</span></span>

2. <span data-ttu-id="b0eb6-169">Il passe au deuxième caractère et réussit à mettre en correspondance la chaîne « ab » avec l’expression `\1b`, ou « ab ».</span><span class="sxs-lookup"><span data-stu-id="b0eb6-169">It advances to the second character, and successfully matches the string "ab" with the expression `\1b`, or "ab".</span></span> <span data-ttu-id="b0eb6-170">Il affecte ensuite le résultat, « ab », à `\1`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-170">It then assigns the result, "ab" to `\1`.</span></span>

3. <span data-ttu-id="b0eb6-171">Il passe au quatrième caractère.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-171">It advances to the fourth character.</span></span> <span data-ttu-id="b0eb6-172">L’expression `(?<1>\1b)*` doit être mise en correspondance zéro fois ou plus, donc il réussit à mettre en correspondance la chaîne « abb » avec l’expression `\1b`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-172">The expression `(?<1>\1b)*` is to be matched zero or more times, so it successfully matches the string "abb" with the expression `\1b`.</span></span> <span data-ttu-id="b0eb6-173">Il réaffecte le résultat, « abb », à `\1`.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-173">It assigns the result, "abb", back to `\1`.</span></span>

<span data-ttu-id="b0eb6-174">Dans cet exemple, `*` est un quantificateur en boucle : il est évalué à plusieurs reprises jusqu’à ce que le moteur d’expression régulière ne puisse pas mettre en correspondance le modèle qu’il définit.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-174">In this example, `*` is a looping quantifier -- it is evaluated repeatedly until the regular expression engine cannot match the pattern it defines.</span></span> <span data-ttu-id="b0eb6-175">Les quantificateurs en boucle ne suppriment pas les définitions de groupe.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-175">Looping quantifiers do not clear group definitions.</span></span>

<span data-ttu-id="b0eb6-176">Si un groupe n’a capturé aucune sous-chaîne, aucune référence arrière à ce groupe n’est définie et aucune correspondance n’est trouvée.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-176">If a group has not captured any substrings, a backreference to that group is undefined and never matches.</span></span> <span data-ttu-id="b0eb6-177">Ce point est illustré par le modèle d’expression régulière `\b(\p{Lu}{2})(\d{2})?(\p{Lu}{2})\b` qui est défini comme suit :</span><span class="sxs-lookup"><span data-stu-id="b0eb6-177">This is illustrated by the regular expression pattern `\b(\p{Lu}{2})(\d{2})?(\p{Lu}{2})\b`, which is defined as follows:</span></span>

|<span data-ttu-id="b0eb6-178">Motif</span><span class="sxs-lookup"><span data-stu-id="b0eb6-178">Pattern</span></span>|<span data-ttu-id="b0eb6-179">Description</span><span class="sxs-lookup"><span data-stu-id="b0eb6-179">Description</span></span>|
|-------------|-----------------|
|`\b`|<span data-ttu-id="b0eb6-180">Commencer la correspondance à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-180">Begin the match on a word boundary.</span></span>|
|`(\p{Lu}{2})`|<span data-ttu-id="b0eb6-181">Mettre en correspondance deux lettres majuscules.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-181">Match two uppercase letters.</span></span> <span data-ttu-id="b0eb6-182">Il s'agit du premier groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-182">This is the first capturing group.</span></span>|
|`(\d{2})?`|<span data-ttu-id="b0eb6-183">Mettre en correspondance zéro ou une occurrence de deux chiffres décimaux.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-183">Match zero or one occurrence of two decimal digits.</span></span> <span data-ttu-id="b0eb6-184">Il s'agit du deuxième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-184">This is the second capturing group.</span></span>|
|`(\p{Lu}{2})`|<span data-ttu-id="b0eb6-185">Mettre en correspondance deux lettres majuscules.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-185">Match two uppercase letters.</span></span> <span data-ttu-id="b0eb6-186">Il s'agit du troisième groupe de capture.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-186">This is the third capturing group.</span></span>|
|`\b`|<span data-ttu-id="b0eb6-187">Terminer la correspondance à la limite d'un mot.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-187">End the match on a word boundary.</span></span>|

<span data-ttu-id="b0eb6-188">Une chaîne d’entrée peut mettre en correspondre cette expression régulière même si les deux chiffres décimaux définis par le deuxième groupe de capture ne sont pas présents.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-188">An input string can match this regular expression even if the two decimal digits that are defined by the second capturing group are not present.</span></span> <span data-ttu-id="b0eb6-189">L’exemple suivant montre que, même si la mise en correspondance réussit, un groupe de capture vide est trouvé entre deux groupes de capture trouvés.</span><span class="sxs-lookup"><span data-stu-id="b0eb6-189">The following example shows that even though the match is successful, an empty capturing group is found between two successful capturing groups.</span></span>

[!code-csharp[RegularExpressions.Language.Backreferences#5](../../../samples/snippets/csharp/VS_Snippets_CLR/regularexpressions.language.backreferences/cs/backreference5.cs#5)]
[!code-vb[RegularExpressions.Language.Backreferences#5](../../../samples/snippets/visualbasic/VS_Snippets_CLR/regularexpressions.language.backreferences/vb/backreference5.vb#5)]

## <a name="see-also"></a><span data-ttu-id="b0eb6-190">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="b0eb6-190">See also</span></span>

- [<span data-ttu-id="b0eb6-191">Langage des expressions régulières - Aide-mémoire</span><span class="sxs-lookup"><span data-stu-id="b0eb6-191">Regular Expression Language - Quick Reference</span></span>](../../../docs/standard/base-types/regular-expression-language-quick-reference.md)
