---
title: Compilation et réutilisation dans les expressions régulières
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- parsing text with regular expressions, compilation
- searching with regular expressions, compilation
- .NET Framework regular expressions, engines
- .NET Framework regular expressions, compilation
- regular expressions, compilation
- compilation, regular expressions
- pattern-matching with regular expressions, compilation
- regular expressions, engines
ms.assetid: 182ec76d-5a01-4d73-996c-0b0d14fcea18
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: ca2dfcfbb5407be2727343a5b3a5b429af83ae20
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/28/2019
ms.locfileid: "64634585"
---
# <a name="compilation-and-reuse-in-regular-expressions"></a><span data-ttu-id="9d9ee-102">Compilation et réutilisation dans les expressions régulières</span><span class="sxs-lookup"><span data-stu-id="9d9ee-102">Compilation and Reuse in Regular Expressions</span></span>
<span data-ttu-id="9d9ee-103">Vous pouvez optimiser les performances des applications qui utilisent très souvent des expressions régulières en comprenant comment le moteur d’expression régulière compile les expressions et comment les expressions régulières sont mises en cache.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-103">You can optimize the performance of applications that make extensive use of regular expressions by understanding how the regular expression engine compiles expressions and by understanding how regular expressions are cached.</span></span> <span data-ttu-id="9d9ee-104">Cette rubrique décrit la compilation et la mise en cache.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-104">This topic discusses both compilation and caching.</span></span>  
  
## <a name="compiled-regular-expressions"></a><span data-ttu-id="9d9ee-105">Expressions régulières compilées</span><span class="sxs-lookup"><span data-stu-id="9d9ee-105">Compiled Regular Expressions</span></span>  
 <span data-ttu-id="9d9ee-106">Par défaut, le moteur d’expression régulière compile une expression régulière en une séquence d’instructions internes (il s’agit des codes généraux différents de MSIL, ou Microsoft Intermediate Language).</span><span class="sxs-lookup"><span data-stu-id="9d9ee-106">By default, the regular expression engine compiles a regular expression to a sequence of internal instructions (these are high-level codes that are different from Microsoft intermediate language, or MSIL).</span></span> <span data-ttu-id="9d9ee-107">Quand le moteur exécute une expression régulière, il interprète les codes internes.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-107">When the engine executes a regular expression, it interprets the internal codes.</span></span>  
  
 <span data-ttu-id="9d9ee-108">Si un objet <xref:System.Text.RegularExpressions.Regex> est construit avec l’option <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType>, il compile l’expression régulière en code MSIL explicite plutôt qu’en instructions internes d’expression régulière générale.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-108">If a <xref:System.Text.RegularExpressions.Regex> object is constructed with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option, it compiles the regular expression to explicit MSIL code instead of high-level regular expression internal instructions.</span></span> <span data-ttu-id="9d9ee-109">Ainsi, le compilateur juste-à-temps (JIT) de .NET convertit l’expression en code machine natif pour de meilleures performances.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-109">This allows .NET's just-in-time (JIT) compiler to convert the expression to native machine code for higher performance.</span></span>  
  
<span data-ttu-id="9d9ee-110">Toutefois, le MSIL généré ne peut pas être déchargé.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-110">However, generated MSIL cannot be unloaded.</span></span> <span data-ttu-id="9d9ee-111">La seule façon de décharger du code consiste à décharger un domaine d’application dans son intégralité (autrement dit, à décharger tout le code de vos applications).</span><span class="sxs-lookup"><span data-stu-id="9d9ee-111">The only way to unload code is to unload an entire application domain (that is, to unload all of your application's code.).</span></span> <span data-ttu-id="9d9ee-112">En effet, une fois qu’une expression régulière est compilée avec l’option <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType>, .NET ne libère jamais les ressources utilisées par l’expression compilée, même si l’expression régulière a été créée par un objet <xref:System.Text.RegularExpressions.Regex> lui-même libéré par nettoyage de la mémoire.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-112">Effectively, once a regular expression is compiled with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option,   never releases the resources used by the compiled expression, even if the regular expression was created by a <xref:System.Text.RegularExpressions.Regex> object that is itself released to garbage collection.</span></span>  
  
 <span data-ttu-id="9d9ee-113">Vous devez veiller à limiter le nombre d’expressions régulières différentes que vous compilez avec l’option <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> pour éviter de consommer trop de ressources.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-113">You must be careful to limit the number of different regular expressions you compile with the <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> option to avoid consuming too many resources.</span></span> <span data-ttu-id="9d9ee-114">Si une application doit utiliser un nombre important ou illimité d’expressions régulières, chaque expression doit être interprétée, et non compilée.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-114">If an application must use a large or unbounded number of regular expressions, each expression should be interpreted, not compiled.</span></span> <span data-ttu-id="9d9ee-115">Toutefois, si un petit nombre d’expressions régulières est utilisé à plusieurs reprises, elles doivent être compilées avec l’option <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> pour de meilleures performances.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-115">However, if a small number of regular expressions are used repeatedly, they should be compiled with <xref:System.Text.RegularExpressions.RegexOptions.Compiled?displayProperty=nameWithType> for better performance.</span></span> <span data-ttu-id="9d9ee-116">Une alternative serait d’utiliser des expressions régulières précompilées.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-116">An alternative is to use precompiled regular expressions.</span></span> <span data-ttu-id="9d9ee-117">Vous pouvez compiler l’ensemble de vos expressions dans une DLL réutilisable à l’aide de la méthode <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A>.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-117">You can compile all of your expressions into a reusable DLL by using the <xref:System.Text.RegularExpressions.Regex.CompileToAssembly%2A> method.</span></span> <span data-ttu-id="9d9ee-118">Cela évite d’avoir à compiler au moment de l’exécution tout en continuant à tirer parti de la rapidité des expressions régulières compilées.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-118">This avoids the need to compile at runtime while still benefiting from the speed of compiled regular expressions.</span></span>  
  
## <a name="the-regular-expressions-cache"></a><span data-ttu-id="9d9ee-119">Cache des expressions régulières</span><span class="sxs-lookup"><span data-stu-id="9d9ee-119">The Regular Expressions Cache</span></span>  
 <span data-ttu-id="9d9ee-120">Pour améliorer les performances, le moteur d’expression régulière gère un cache à l’échelle de l’application des expressions régulières compilées.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-120">To improve performance, the regular expression engine maintains an application-wide cache of compiled regular expressions.</span></span> <span data-ttu-id="9d9ee-121">Ce cache stocke les modèles d’expression régulière utilisés uniquement dans les appels de méthode statique.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-121">The cache stores regular expression patterns that are used only in static method calls.</span></span> <span data-ttu-id="9d9ee-122">(Les modèles d’expression régulière fournis aux méthodes d’instance ne sont pas mis en cache.) Ainsi, la nécessité de réanalyser une expression en code d’octet principal à chacune de ses utilisations est évité.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-122">(Regular expression patterns supplied to instance methods are not cached.) This avoids the need to reparse an expression into high-level byte code each time it is used.</span></span>  
  
 <span data-ttu-id="9d9ee-123">Le nombre maximal d’expressions régulières mises en cache est déterminé par la valeur de la propriété <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> `static` (`Shared` en Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="9d9ee-123">The maximum number of cached regular expressions is determined by the value of the `static` (`Shared` in Visual Basic) <xref:System.Text.RegularExpressions.Regex.CacheSize%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="9d9ee-124">Par défaut, le moteur d’expression régulière met en cache jusqu’à 15 expressions régulières compilées.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-124">By default, the regular expression engine caches up to 15 compiled regular expressions.</span></span> <span data-ttu-id="9d9ee-125">Si le nombre d’expressions régulières compilées dépasse la taille du cache, l’expression régulière la plus anciennement utilisée est ignorée et la nouvelle expression régulière est mise en cache.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-125">If the number of compiled regular expressions exceeds the cache size, the least recently used regular expression is discarded and the new regular expression is cached.</span></span>  
  
 <span data-ttu-id="9d9ee-126">Votre application peut tirer parti des expressions régulières précompilées de l’une des deux manières suivantes :</span><span class="sxs-lookup"><span data-stu-id="9d9ee-126">Your application can take advantage of precompiled regular expressions in one of the following two ways:</span></span>  
  
- <span data-ttu-id="9d9ee-127">En utilisant une méthode statique de l’objet <xref:System.Text.RegularExpressions.Regex> pour définir l’expression régulière.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-127">By using a static method of the <xref:System.Text.RegularExpressions.Regex> object to define the regular expression.</span></span> <span data-ttu-id="9d9ee-128">Si vous utilisez un modèle d’expression régulière qui a déjà été défini dans un autre appel de méthode statique, le moteur d’expression régulière le récupère à partir du cache.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-128">If you are using a regular expression pattern that has already been defined in another static method call, the regular expression engine will retrieve it from the cache.</span></span> <span data-ttu-id="9d9ee-129">Si ce n’est pas le cas, le moteur compile l’expression régulière et l’ajoute au cache.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-129">If not, the engine will compile the regular expression and add it to the cache.</span></span>  
  
- <span data-ttu-id="9d9ee-130">En réutilisant un objet <xref:System.Text.RegularExpressions.Regex> existant tant que son modèle d’expression régulière est nécessaire.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-130">By reusing an existing <xref:System.Text.RegularExpressions.Regex> object as long as its regular expression pattern is needed.</span></span>  
  
 <span data-ttu-id="9d9ee-131">En raison de la surcharge liée à l’instanciation d’objets et à la compilation d’expressions régulières, la création et la destruction rapide d’un grand nombre d’objets <xref:System.Text.RegularExpressions.Regex> est un processus très coûteux.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-131">Because of the overhead of object instantiation and regular expression compilation, creating and rapidly destroying numerous <xref:System.Text.RegularExpressions.Regex> objects is a very expensive process.</span></span> <span data-ttu-id="9d9ee-132">Pour les applications qui utilisent un grand nombre d’expressions régulières différentes, vous pouvez optimiser les performances en utilisant des appels de méthodes `Regex` statiques et en augmentant éventuellement la taille du cache des expressions régulières.</span><span class="sxs-lookup"><span data-stu-id="9d9ee-132">For applications that use a large number of different regular expressions, you can optimize performance by using calls to static `Regex` methods and possibly by increasing the size of the regular expression cache.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9d9ee-133">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="9d9ee-133">See also</span></span>

- [<span data-ttu-id="9d9ee-134">Expressions régulières .NET</span><span class="sxs-lookup"><span data-stu-id="9d9ee-134">.NET Regular Expressions</span></span>](../../../docs/standard/base-types/regular-expressions.md)
