---
title: Sélectionner la version .NET Core à utiliser
description: Découvrez comment .NET Core recherche et choisit automatiquement les versions du runtime pour votre programme. En outre, cet article vous apprend à forcer l’utiliser d’une version spécifique.
author: billwagner
ms.author: wiwagn
ms.date: 06/27/2018
ms.custom: seodec18
ms.openlocfilehash: 3e9a60221a5769d124bcc137d9401367a7713abb
ms.sourcegitcommit: ccd8c36b0d74d99291d41aceb14cf98d74dc9d2b
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 12/10/2018
ms.locfileid: "53127236"
---
# <a name="select-the-net-core-version-to-use"></a><span data-ttu-id="a1a2d-104">Sélectionner la version .NET Core à utiliser</span><span class="sxs-lookup"><span data-stu-id="a1a2d-104">Select the .NET Core version to use</span></span>

[!INCLUDE [topic-appliesto-net-core-2plus](../../../includes/topic-appliesto-net-core-2plus.md)]

<span data-ttu-id="a1a2d-105">Cet article explique les stratégies utilisées par les outils .NET Core, le kit SDK et le runtime pour sélectionner les versions.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-105">This article explains the policies used by the .NET Core tools, SDK, and runtime for selecting versions.</span></span> <span data-ttu-id="a1a2d-106">Ces stratégies concilient l’exécution des applications avec les versions spécifiées et la facilité de mise à niveau des machines des développeurs et des utilisateurs finaux.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-106">These policies provide a balance between running applications using the specified versions and enabling ease of upgrading both developer and end-user machines.</span></span> <span data-ttu-id="a1a2d-107">Voici les actions effectuées par ces stratégies :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-107">These policies perform the following actions:</span></span>

- <span data-ttu-id="a1a2d-108">Déploiement facile et efficace de .NET Core, notamment des mises à jour de sécurité et de fiabilité.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-108">Easy and efficient deployment of .NET Core, including security and reliability updates.</span></span>
- <span data-ttu-id="a1a2d-109">Utilisation des outils et des commandes les plus récents, indépendamment du runtime cible.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-109">Use the latest tools and commands independent of target runtime.</span></span>

<span data-ttu-id="a1a2d-110">La sélection de version s’opère dans les cas suivants :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-110">Version selection occurs:</span></span>

- <span data-ttu-id="a1a2d-111">Quand vous exécutez une commande du Kit SDK, [celui-ci utilise la dernière version installée](#the-sdk-uses-the-latest-installed-version).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-111">When you run an SDK command, [the SDK uses the latest installed version](#the-sdk-uses-the-latest-installed-version).</span></span>
- <span data-ttu-id="a1a2d-112">Quand vous générez un assembly, [les monikers de framework cible définissent les API au moment de la génération](#target-framework-monikers-define-build-time-apis).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-112">When you build an assembly, [target framework monikers define build time APIs](#target-framework-monikers-define-build-time-apis).</span></span>
- <span data-ttu-id="a1a2d-113">Quand vous exécutez une application .NET Core, [les applications dépendantes de la version cible de .Net Framework font l’objet d’une restauration par progression](#framework-dependent-apps-roll-forward).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-113">When you run a .NET Core application, [target framework dependent apps roll forward](#framework-dependent-apps-roll-forward).</span></span>
- <span data-ttu-id="a1a2d-114">Quand vous publiez une application autonome, [les déploiements autonomes incluent le runtime sélectionné](#self-contained-deployments-include-the-selected-runtime).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-114">When you publish a self-contained application, [self-contained deployments include the selected runtime](#self-contained-deployments-include-the-selected-runtime).</span></span>

<span data-ttu-id="a1a2d-115">Le reste de ce document examine ces quatre scénarios.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-115">The rest of this document examines those four scenarios.</span></span>

## <a name="the-sdk-uses-the-latest-installed-version"></a><span data-ttu-id="a1a2d-116">Le kit SDK utilise la dernière version installée</span><span class="sxs-lookup"><span data-stu-id="a1a2d-116">The SDK uses the latest installed version</span></span>

<span data-ttu-id="a1a2d-117">Les commandes du kit SDK incluent `dotnet new` et `dotnet run`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-117">SDK commands include `dotnet new` and `dotnet run`.</span></span> <span data-ttu-id="a1a2d-118">L’interface CLI .NET Core doit choisir une version du Kit SDK pour chaque commande `dotnet`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-118">The .NET Core CLI must choose an SDK version for every `dotnet` command.</span></span> <span data-ttu-id="a1a2d-119">Par défaut, elle utilise le dernier Kit SDK installé sur la machine, même si :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-119">It uses the latest SDK installed on the machine by default, even if:</span></span>

* <span data-ttu-id="a1a2d-120">Le projet cible une version antérieure du runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-120">The project targets an earlier version of the .NET Core runtime.</span></span>
* <span data-ttu-id="a1a2d-121">La dernière version du Kit SDK .NET Core est une préversion.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-121">The latest version of the .NET Core SDK is a preview version.</span></span>

<span data-ttu-id="a1a2d-122">Vous pouvez tirer parti des fonctionnalités et des améliorations du dernier kit SDK tout en ciblant des versions antérieures du runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-122">You can take advantage of the latest SDK features and improvements while targeting earlier .NET Core runtime versions.</span></span> <span data-ttu-id="a1a2d-123">Vous pouvez cibler plusieurs versions du runtime de .NET Core dans différents projets en utilisant les mêmes outils du kit SDK pour tous les projets.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-123">You can target multiple runtime versions of .NET Core on different projects, using the same SDK tools for all projects.</span></span>

<span data-ttu-id="a1a2d-124">À de rares occasions, vous pouvez être amené à utiliser une version antérieure du kit SDK.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-124">On rare occasions, you may need to use an earlier version of the SDK.</span></span> <span data-ttu-id="a1a2d-125">Vous devez dans ce cas spécifier cette version dans un [ fichier *global.json*](../tools/global-json.md).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-125">You specify that version in a [*global.json* file](../tools/global-json.md).</span></span> <span data-ttu-id="a1a2d-126">La stratégie « utiliser la dernière version » signifie que vous utilisez uniquement *global.json* pour spécifier une version du kit SDK .NET Core antérieure à la dernière version installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-126">The "use latest" policy means you only use *global.json* to specify a .NET Core SDK version earlier than the latest installed version.</span></span>

<span data-ttu-id="a1a2d-127">*global.json* peut être placé n’importe où dans la hiérarchie des fichiers.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-127">*global.json* can be placed anywhere in the file hierarchy.</span></span> <span data-ttu-id="a1a2d-128">L’interface CLI effectue une recherche vers le haut dans le répertoire du projet et s’arrête au premier fichier *global.json* trouvé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-128">The CLI searches upward from the project directory for the first *global.json* it finds.</span></span> <span data-ttu-id="a1a2d-129">Vous pouvez contrôler les projets auxquels un fichier *global.json* donné s’applique par son emplacement dans le système de fichiers.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-129">You control which projects a given *global.json* applies to by its place in the file system.</span></span> <span data-ttu-id="a1a2d-130">L’interface CLI .NET recherche un fichier *global.json* de manière itérative en parcourant le chemin de bas en haut dans le répertoire de travail actif.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-130">The .NET CLI searches for a *global.json* file iteratively navigating the path upward from the current working directory.</span></span> <span data-ttu-id="a1a2d-131">Le premier fichier *global.json* trouvé spécifie la version utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-131">The first *global.json* file found specifies the version used.</span></span> <span data-ttu-id="a1a2d-132">Si cette version est installée, elle est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-132">If that version is installed, that version is used.</span></span> <span data-ttu-id="a1a2d-133">Si le kit SDK spécifié dans le fichier *global.json* est introuvable, l’interface CLI .NET restaure par progression le dernier kit SDK installé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-133">If the SDK specified in the *global.json* is not found, the .NET CLI rolls forward to the latest SDK installed.</span></span> <span data-ttu-id="a1a2d-134">La restauration par progression correspond au comportement par défaut, quand aucun fichier *global.json* n’est trouvé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-134">Roll-forward is the same as the default behavior, when no *global.json* file is found.</span></span>

<span data-ttu-id="a1a2d-135">L’exemple suivant présente la syntaxe du fichier *global.json* :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-135">The following example shows the *global.json* syntax:</span></span>

``` json
{
  "sdk": {
    "version": "2.0.0"
  }
}
```

<span data-ttu-id="a1a2d-136">Le processus de sélection d’une version du kit SDK est le suivant :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-136">The process for selecting an SDK version is:</span></span>

1. <span data-ttu-id="a1a2d-137">`dotnet` recherche un fichier *global.json* en remontant de manière itérative le chemin dans le répertoire de travail actif.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-137">`dotnet` searches for a *global.json* file iteratively reverse-navigating the path upward from the current working directory.</span></span>
1. <span data-ttu-id="a1a2d-138">`dotnet` utilise le kit SDK spécifié dans le premier fichier *global.json* trouvé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-138">`dotnet` uses the SDK specified in the first *global.json* found.</span></span>
1. <span data-ttu-id="a1a2d-139">`dotnet` utilise la dernière version du kit SDK installé si aucun fichier *global.json* n’est trouvé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-139">`dotnet` uses the latest installed SDK if no *global.json* is found.</span></span>

<span data-ttu-id="a1a2d-140">Pour plus d’informations sur la sélection d’une version du kit SDK, consultez la section [Règles de correspondance](../tools/global-json.md#matching-rules) dans l’article sur *global.json*.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-140">You can learn more about selecting an SDK version in the [Matching rules](../tools/global-json.md#matching-rules) section of the article on *global.json*.</span></span>

## <a name="target-framework-monikers-define-build-time-apis"></a><span data-ttu-id="a1a2d-141">Les monikers de framework cible définissent les API au moment de la génération</span><span class="sxs-lookup"><span data-stu-id="a1a2d-141">Target Framework Monikers define build time APIs</span></span>

<span data-ttu-id="a1a2d-142">Vous générez votre projet par rapport aux API définies dans un **moniker de framework cible** (TFM).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-142">You build your project against APIs defined in a **Target Framework Moniker** (TFM).</span></span> <span data-ttu-id="a1a2d-143">Vous devez spécifier la [version cible de .Net Framework](../../standard/frameworks.md) dans le fichier projet.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-143">You specify the [target framework](../../standard/frameworks.md) in the project file.</span></span> <span data-ttu-id="a1a2d-144">Définissez l’élément `TargetFramework` dans votre fichier projet comme indiqué dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-144">Set the `TargetFramework` element in your project file as shown in the following example:</span></span>

``` xml
<TargetFramework>netcoreapp2.0</TargetFramework>
```

<span data-ttu-id="a1a2d-145">Vous pouvez générer votre projet par rapport à plusieurs TFM.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-145">You may build your project against multiple TFMs.</span></span> <span data-ttu-id="a1a2d-146">S’il est plus courant de définir plusieurs versions cibles de .Net Framework pour les bibliothèques, cela est également possible dans le cas des applications.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-146">Setting multiple target frameworks is more common for libraries but can be done with applications as well.</span></span> <span data-ttu-id="a1a2d-147">Pour cela, vous devez spécifier une propriété `TargetFrameworks` (pluriel de `TargetFramework`).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-147">You specify a `TargetFrameworks` property (plural of `TargetFramework`).</span></span> <span data-ttu-id="a1a2d-148">Les versions cibles de .Net Framework sont séparées par un point-virgule, comme le montre l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-148">The target frameworks are semicolon-delimited as shown in the following example:</span></span>

``` xml
<TargetFrameworks>netcoreapp2.0;net47</TargetFrameworks>
```

<span data-ttu-id="a1a2d-149">Un kit SDK donné prend en charge un ensemble fixe de versions de .Net Framework, plafonné à la version cible de .Net Framework du runtime qu’il intègre.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-149">A given SDK supports a fixed set of frameworks, capped to the target framework of the runtime it ships with.</span></span> <span data-ttu-id="a1a2d-150">Par exemple, le kit SDK .NET Core 2.0 comprend le runtime .NET Core 2.0, qui est une implémentation de la version cible de .Net Framework `netcoreapp2.0`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-150">For example, the .NET Core 2.0 SDK includes the .NET Core 2.0 runtime, which is an implementation of the `netcoreapp2.0` target framework.</span></span> <span data-ttu-id="a1a2d-151">Le kit SDK .NET Core 2.0 prend en charge `netcoreapp1.0`, `netcoreapp1.1`, et `netcoreapp2.0`, mais pas `netcoreapp2.1` (ou version ultérieure).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-151">The .NET Core 2.0 SDK supports `netcoreapp1.0`, `netcoreapp1.1`, and `netcoreapp2.0` but not `netcoreapp2.1` (or higher).</span></span> <span data-ttu-id="a1a2d-152">L’installation du kit SDK .NET Core 2.1 vise à générer pour `netcoreapp2.1`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-152">You install the .NET Core 2.1 SDK to build for `netcoreapp2.1`.</span></span>

<span data-ttu-id="a1a2d-153">Les versions cibles de .Net Framework Standard sont également plafonnées à la version cible de .Net Framework du runtime intégré au kit SDK.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-153">.NET Standard target frameworks are also capped to the target framework of the runtime the SDK ships with.</span></span> <span data-ttu-id="a1a2d-154">Le kit SDK .NET Core 2.0 est plafonné à `netstandard2.0`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-154">The .NET Core 2.0 SDK is capped to `netstandard2.0`.</span></span>

## <a name="framework-dependent-apps-roll-forward"></a><span data-ttu-id="a1a2d-155">Les applications dépendantes du framework font l’objet d’une restauration par progression</span><span class="sxs-lookup"><span data-stu-id="a1a2d-155">Framework-dependent apps roll forward</span></span>

<span data-ttu-id="a1a2d-156">Lorsque vous exécutez une application à partir d’une source avec [`dotnet run`](../tools/dotnet-run.md), à partir d’un [**déploiement dépendant du framework**](../deploying/index.md#framework-dependent-deployments-fdd) avec [`dotnet myapp.dll`](../tools/dotnet.md#description), ou à partir d’un [**exécutable dépendant du framework**](../deploying/index.md#framework-dependent-executables-fde) avec `myapp.exe`, l’exécutable `dotnet` est l’**hôte** de l’application.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-156">When you run an application from source with [`dotnet run`](../tools/dotnet-run.md), from a [**framework-dependent deployment**](../deploying/index.md#framework-dependent-deployments-fdd) with [`dotnet myapp.dll`](../tools/dotnet.md#description), or from a [**framework-dependent executable**](../deploying/index.md#framework-dependent-executables-fde) with `myapp.exe`, the `dotnet` executable is the **host** for the application.</span></span>

<span data-ttu-id="a1a2d-157">L’hôte choisit la dernière version de correctif installée sur la machine.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-157">The host chooses the latest patch version installed on the machine.</span></span> <span data-ttu-id="a1a2d-158">Par exemple, si vous avez spécifié `netcoreapp2.0` dans votre fichier projet et que `2.0.4` est le dernier runtime .NET installé, le runtime `2.0.4` est utilisé.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-158">For example, if you specified `netcoreapp2.0` in your project file, and `2.0.4` is the latest .NET runtime installed, the `2.0.4` runtime is used.</span></span>

<span data-ttu-id="a1a2d-159">Si aucune version acceptable de `2.0.*` n’est trouvée, une nouvelle version `2.*` est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-159">If no acceptable `2.0.*` version is found, a new `2.*` version is used.</span></span> <span data-ttu-id="a1a2d-160">Par exemple, si vous avez spécifié `netcoreapp2.0` et que seule la version `2.1.0` est installée, l’application s’exécute en utilisant le runtime `2.1.0`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-160">For example, if you specified `netcoreapp2.0` and only `2.1.0` is installed, the application runs using the `2.1.0` runtime.</span></span> <span data-ttu-id="a1a2d-161">Ce comportement est appelé « restauration par progression d’une version mineure ».</span><span class="sxs-lookup"><span data-stu-id="a1a2d-161">This behavior is referred to as "minor version roll-forward."</span></span> <span data-ttu-id="a1a2d-162">Les versions antérieures ne sont pas non plus prises en considération.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-162">Lower versions also won't be considered.</span></span> <span data-ttu-id="a1a2d-163">Quand aucun runtime acceptable n’est installé, l’application ne s’exécute pas.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-163">When no acceptable runtime is installed, the application won't run.</span></span>

<span data-ttu-id="a1a2d-164">Voici quelques exemples d’utilisation pour illustrer le comportement :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-164">A few usage examples demonstrate the behavior:</span></span>

- <span data-ttu-id="a1a2d-165">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-165">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-166">La version 2.0.5 est la version de correctif installée la plus élevée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-166">2.0.5 is the highest patch version installed.</span></span> <span data-ttu-id="a1a2d-167">La version 2.0.5 est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-167">2.0.5 is used.</span></span>
- <span data-ttu-id="a1a2d-168">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-168">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-169">Aucune version 2.0.\* n’est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-169">No 2.0.\* versions are installed.</span></span> <span data-ttu-id="a1a2d-170">La version 1.1.1 correspond à la version de runtime installée la plus élevée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-170">1.1.1 is the highest runtime installed.</span></span> <span data-ttu-id="a1a2d-171">Un message d’erreur s’affiche.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-171">An error message is displayed.</span></span>
- <span data-ttu-id="a1a2d-172">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-172">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-173">La version 2.0.0 correspond à la version installée la plus élevée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-173">2.0.0 is the highest version installed.</span></span> <span data-ttu-id="a1a2d-174">Un message d’erreur s’affiche.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-174">An error message is displayed.</span></span>
- <span data-ttu-id="a1a2d-175">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-175">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-176">Aucune version 2.0.\* n’est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-176">No 2.0.\* versions are installed.</span></span> <span data-ttu-id="a1a2d-177">La version 2.2.2 correspond à la version de runtime 2.x installée la plus élevée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-177">2.2.2 is the highest 2.x runtime version installed.</span></span> <span data-ttu-id="a1a2d-178">La version 2.2.2 est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-178">2.2.2 is used.</span></span>
- <span data-ttu-id="a1a2d-179">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-179">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-180">Aucune version 2.x n’est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-180">No 2.x versions are installed.</span></span> <span data-ttu-id="a1a2d-181">La version 3.0.0 (version non disponible actuellement) est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-181">3.0.0 (not a currently available version) is installed.</span></span> <span data-ttu-id="a1a2d-182">Un message d’erreur s’affiche.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-182">An error message is displayed.</span></span>

<span data-ttu-id="a1a2d-183">La restauration par progression de la version mineure présente un effet secondaire qui peut toucher les utilisateurs finaux.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-183">Minor version roll-forward has one side-effect that may affect end users.</span></span> <span data-ttu-id="a1a2d-184">Penchons-nous sur le scénario suivant :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-184">Consider the following scenario:</span></span>

- <span data-ttu-id="a1a2d-185">La version 2.0.4 est requise.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-185">2.0.4 is required.</span></span> <span data-ttu-id="a1a2d-186">Aucune version 2.0.\* n’est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-186">No 2.0.\* versions are installed.</span></span> <span data-ttu-id="a1a2d-187">La version 2.2.2 est installée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-187">2.2.2 is installed.</span></span> <span data-ttu-id="a1a2d-188">La version 2.2.2 est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-188">2.2.2 is used.</span></span>
- <span data-ttu-id="a1a2d-189">La version 2.0.5 est installée ultérieurement.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-189">2.0.5 is later installed.</span></span> <span data-ttu-id="a1a2d-190">La version 2.0.5 sera utilisée pour les lancements suivants de l’application, et non la version 2.2.2.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-190">2.0.5 will be used for subsequent application launches, not 2.2.2.</span></span> <span data-ttu-id="a1a2d-191">Le dernier correctif de la version mineure requise est préféré à une version mineure supérieure.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-191">The latest patch of the required minor version is preferred over a higher minor version.</span></span>
- <span data-ttu-id="a1a2d-192">Il est possible que les versions 2.0.5 et 2.2.2 se comportent différemment, en particulier dans certains scénarios comme la sérialisation de données binaires.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-192">It's possible that 2.0.5 and 2.2.2 behave differently, particularly for scenarios like serializing binary data.</span></span>

## <a name="self-contained-deployments-include-the-selected-runtime"></a><span data-ttu-id="a1a2d-193">Les déploiements autonomes incluent le runtime sélectionné</span><span class="sxs-lookup"><span data-stu-id="a1a2d-193">Self-contained deployments include the selected runtime</span></span>

<span data-ttu-id="a1a2d-194">Vous pouvez publier une application en tant que [**distribution autonome**](../deploying/index.md#self-contained-deployments-scd).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-194">You can publish an application as a [**self-contained distribution**](../deploying/index.md#self-contained-deployments-scd).</span></span> <span data-ttu-id="a1a2d-195">Cette approche regroupe le runtime et les bibliothèques .NET Core avec votre application.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-195">This approach bundles the .NET Core runtime and libraries with your application.</span></span> <span data-ttu-id="a1a2d-196">Les déploiements autonomes ne dépendent pas des environnements d’exécution.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-196">Self-contained deployments don't have a dependency on runtime environments.</span></span> <span data-ttu-id="a1a2d-197">La sélection de la version du runtime se produit au moment de la publication, et non au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-197">Runtime version selection occurs at publishing time, not run time.</span></span>

<span data-ttu-id="a1a2d-198">Le processus de publication sélectionne la dernière version de correctif de la famille de runtime donnée.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-198">The publishing process selects the latest patch version of the given runtime family.</span></span> <span data-ttu-id="a1a2d-199">Par exemple, `dotnet publish` sélectionne .NET Core 2.0.4 s’il s’agit de la dernière version de correctif de la famille du runtime .NET Core 2.0.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-199">For example, `dotnet publish` will select .NET Core 2.0.4 if it is the latest patch version in the .NET Core 2.0 runtime family.</span></span> <span data-ttu-id="a1a2d-200">La version cible de .Net Framework (y compris les derniers correctifs de sécurité installés) est empaquetée avec l’application.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-200">The target framework (including the latest installed security patches) is packaged with the application.</span></span>

<span data-ttu-id="a1a2d-201">Si la version minimale spécifiée pour une application n’est pas satisfaire, il s’agit d’une erreur.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-201">It's an error if the minimum version specified for an application isn't satisfied.</span></span> <span data-ttu-id="a1a2d-202">`dotnet publish` se lie à la dernière version de correctif de runtime (au sein d’une famille de version principale.secondaire donnée).</span><span class="sxs-lookup"><span data-stu-id="a1a2d-202">`dotnet publish` binds to the latest runtime patch version (within a given major.minor version family).</span></span> <span data-ttu-id="a1a2d-203">`dotnet publish` ne prend pas en charge la sémantique de restauration par progression de `dotnet run`.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-203">`dotnet publish` doesn't support the roll-forward semantics of `dotnet run`.</span></span> <span data-ttu-id="a1a2d-204">Pour plus d’informations sur les correctifs et les déploiements autonomes, consultez l’article relatif à la [sélection de correctif de runtime](../deploying/runtime-patch-selection.md) dans le déploiement d’applications .NET Core.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-204">For more information about patches and self-contained deployments, see the article on [runtime patch selection](../deploying/runtime-patch-selection.md) in deploying .NET Core applications.</span></span>

<span data-ttu-id="a1a2d-205">Les déploiements autonomes peuvent nécessiter une version de correctif spécifique.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-205">Self-contained deployments may require a specific patch version.</span></span> <span data-ttu-id="a1a2d-206">Vous pouvez remplacer la version de correctif de runtime minimale (par une version supérieure ou inférieure) dans le fichier projet, comme indiqué dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="a1a2d-206">You can override the minimum runtime patch version (to higher or lower versions) in the project file, as shown in the following example:</span></span>

``` xml
<RuntimeFrameworkVersion>2.0.4</RuntimeFrameworkVersion>
```

<span data-ttu-id="a1a2d-207">L’élément `RuntimeFrameworkVersion` remplace la stratégie de version par défaut.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-207">The `RuntimeFrameworkVersion` element  overrides the default version policy.</span></span> <span data-ttu-id="a1a2d-208">Pour les déploiements autonomes, `RuntimeFrameworkVersion` spécifie la version *exacte* du framework du runtime.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-208">For self-contained deployments, the `RuntimeFrameworkVersion` specifies the *exact* runtime framework version.</span></span> <span data-ttu-id="a1a2d-209">Pour les applications dépendantes du framework, `RuntimeFrameworkVersion` spécifie la version *minimale* requise pour le framework du runtime.</span><span class="sxs-lookup"><span data-stu-id="a1a2d-209">For framework-dependent applications, the `RuntimeFrameworkVersion` specifies the *minimum* required runtime framework version.</span></span>
