---
title: Opérateurs de bits et opérateurs logiques en Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 94d43b7bea48d85e612b5c995e20e286f14b024a
ms.sourcegitcommit: 40364ded04fa6cdcb2b6beca7f68412e2e12f633
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 02/28/2019
ms.locfileid: "56976497"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="9305c-102">Opérateurs de bits et opérateurs logiques en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="9305c-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="9305c-103">Comparent des opérateurs logiques `Boolean` expressions et retournent un `Boolean` résultat.</span><span class="sxs-lookup"><span data-stu-id="9305c-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="9305c-104">Le `And`, `Or`, `AndAlso`, `OrElse`, et `Xor` opérateurs sont *binaire* parce qu’ils prennent deux opérandes, tandis que le `Not` opérateur est *unaire* , car il accepte un opérande unique.</span><span class="sxs-lookup"><span data-stu-id="9305c-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="9305c-105">Certains de ces opérateurs peuvent également effectuer des opérations logiques au niveau du bit sur des valeurs intégrales.</span><span class="sxs-lookup"><span data-stu-id="9305c-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="9305c-106">Opérateur logique unaire</span><span class="sxs-lookup"><span data-stu-id="9305c-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="9305c-107">Le [pas d’opérateur](../../../../visual-basic/language-reference/operators/not-operator.md) effectue logique *négation* sur un `Boolean` expression.</span><span class="sxs-lookup"><span data-stu-id="9305c-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="9305c-108">Il génère le contraire logique de son opérande.</span><span class="sxs-lookup"><span data-stu-id="9305c-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="9305c-109">Si l’expression prend la valeur `True`, puis `Not` retourne `False`; si l’expression prend la valeur `False`, puis `Not` retourne `True`.</span><span class="sxs-lookup"><span data-stu-id="9305c-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="9305c-110">L'exemple suivant illustre ce comportement.</span><span class="sxs-lookup"><span data-stu-id="9305c-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="9305c-111">Opérateurs logiques binaires</span><span class="sxs-lookup"><span data-stu-id="9305c-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="9305c-112">Le [opérateur et](../../../../visual-basic/language-reference/operators/and-operator.md) effectue logique *conjonction* sur deux `Boolean` expressions.</span><span class="sxs-lookup"><span data-stu-id="9305c-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="9305c-113">Si les deux expressions ont `True`, puis `And` retourne `True`.</span><span class="sxs-lookup"><span data-stu-id="9305c-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="9305c-114">Si au moins une des expressions a la valeur `False`, puis `And` retourne `False`.</span><span class="sxs-lookup"><span data-stu-id="9305c-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="9305c-115">Le [ou opérateur](../../../../visual-basic/language-reference/operators/or-operator.md) effectue logique *disjonction* ou *inclusion* sur deux `Boolean` expressions.</span><span class="sxs-lookup"><span data-stu-id="9305c-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="9305c-116">Si des expressions a la valeur `True`, ou les deux ont la `True`, puis `Or` retourne `True`.</span><span class="sxs-lookup"><span data-stu-id="9305c-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="9305c-117">Si aucune expression n’a la valeur `True`, `Or` retourne `False`.</span><span class="sxs-lookup"><span data-stu-id="9305c-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="9305c-118">Le [Xor, opérateur](../../../../visual-basic/language-reference/operators/xor-operator.md) effectue logique *exclusion* sur deux `Boolean` expressions.</span><span class="sxs-lookup"><span data-stu-id="9305c-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="9305c-119">Si une seule expression `True`, mais pas les deux, `Xor` retourne `True`.</span><span class="sxs-lookup"><span data-stu-id="9305c-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="9305c-120">Si les deux expressions ont `True` ou les deux ont la `False`, `Xor` retourne `False`.</span><span class="sxs-lookup"><span data-stu-id="9305c-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="9305c-121">L’exemple suivant illustre la `And`, `Or`, et `Xor` opérateurs.</span><span class="sxs-lookup"><span data-stu-id="9305c-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="9305c-122">Opérations logiques de court-circuit</span><span class="sxs-lookup"><span data-stu-id="9305c-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="9305c-123">Le [opérateur AndAlso](../../../../visual-basic/language-reference/operators/andalso-operator.md) est très similaire à la `And` opérateur, car il effectue également une conjonction logique sur deux `Boolean` expressions.</span><span class="sxs-lookup"><span data-stu-id="9305c-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="9305c-124">La principale différence entre les deux est que `AndAlso` présente *court-circuit* comportement.</span><span class="sxs-lookup"><span data-stu-id="9305c-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="9305c-125">Si la première expression dans une `AndAlso` expression prend la valeur `False`, puis la seconde expression n’est pas évaluée, car elle ne peut pas modifier le résultat final, et `AndAlso` retourne `False`.</span><span class="sxs-lookup"><span data-stu-id="9305c-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="9305c-126">De même, le [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) effectue une disjonction logique sur deux court-circuit `Boolean` expressions.</span><span class="sxs-lookup"><span data-stu-id="9305c-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="9305c-127">Si la première expression dans une `OrElse` expression prend la valeur `True`, puis la seconde expression n’est pas évaluée, car elle ne peut pas modifier le résultat final, et `OrElse` retourne `True`.</span><span class="sxs-lookup"><span data-stu-id="9305c-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="9305c-128">Compromis de court-circuit</span><span class="sxs-lookup"><span data-stu-id="9305c-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="9305c-129">Court-circuit peut améliorer les performances en évaluant ne pas une expression qui ne peut pas modifier le résultat de l’opération logique.</span><span class="sxs-lookup"><span data-stu-id="9305c-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="9305c-130">Toutefois, si cette expression effectue des actions supplémentaires, de court-circuit ignore ces actions.</span><span class="sxs-lookup"><span data-stu-id="9305c-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="9305c-131">Par exemple, si l’expression inclut un appel à une `Function` procédure, que procédure n’est pas appelée si l’expression est court-circuitée, et le code supplémentaire contenu dans le `Function` ne s’exécute pas.</span><span class="sxs-lookup"><span data-stu-id="9305c-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="9305c-132">Par conséquent, la fonction peut s’exécuter qu’occasionnellement et ne peut pas être testée correctement.</span><span class="sxs-lookup"><span data-stu-id="9305c-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="9305c-133">Ou la logique du programme peut dépendre du code dans le `Function`.</span><span class="sxs-lookup"><span data-stu-id="9305c-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="9305c-134">L’exemple suivant illustre la différence entre `And`, `Or`et leurs équivalents de court-circuit.</span><span class="sxs-lookup"><span data-stu-id="9305c-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="9305c-135">Dans l’exemple précédent, notez que du code important dans `checkIfValid()` ne s’exécute pas lorsque l’appel est court-circuitée.</span><span class="sxs-lookup"><span data-stu-id="9305c-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="9305c-136">La première `If` instruction appelle `checkIfValid()` bien `12 > 45` retourne `False`, car `And` n’effectue pas de court-circuit.</span><span class="sxs-lookup"><span data-stu-id="9305c-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="9305c-137">La seconde `If` instruction n’appelle pas `checkIfValid()`, car lorsque `12 > 45` retourne `False`, `AndAlso` court-circuite la deuxième expression.</span><span class="sxs-lookup"><span data-stu-id="9305c-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="9305c-138">La troisième `If` instruction appelle `checkIfValid()` bien `12 < 45` retourne `True`, car `Or` n’effectue pas de court-circuit.</span><span class="sxs-lookup"><span data-stu-id="9305c-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="9305c-139">La quatrième `If` instruction n’appelle pas `checkIfValid()`, car lorsque `12 < 45` retourne `True`, `OrElse` court-circuite la deuxième expression.</span><span class="sxs-lookup"><span data-stu-id="9305c-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="9305c-140">Opérations au niveau du bit</span><span class="sxs-lookup"><span data-stu-id="9305c-140">Bitwise Operations</span></span>  
 <span data-ttu-id="9305c-141">Opérations de bits évaluent deux valeurs intégrales sous forme binaire (base 2).</span><span class="sxs-lookup"><span data-stu-id="9305c-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="9305c-142">Elles comparent les bits aux positions correspondantes et ensuite affecter des valeurs en fonction de la comparaison.</span><span class="sxs-lookup"><span data-stu-id="9305c-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="9305c-143">L’exemple suivant illustre la `And` opérateur.</span><span class="sxs-lookup"><span data-stu-id="9305c-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="9305c-144">L’exemple précédent définit la valeur de `x` sur 1.</span><span class="sxs-lookup"><span data-stu-id="9305c-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="9305c-145">Cela se produit pour les raisons suivantes :</span><span class="sxs-lookup"><span data-stu-id="9305c-145">This happens for the following reasons:</span></span>  
  
-   <span data-ttu-id="9305c-146">Les valeurs sont traitées en tant que fichier binaire :</span><span class="sxs-lookup"><span data-stu-id="9305c-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="9305c-147">3 sous forme binaire = 011</span><span class="sxs-lookup"><span data-stu-id="9305c-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="9305c-148">5 sous forme binaire = 101</span><span class="sxs-lookup"><span data-stu-id="9305c-148">5 in binary form = 101</span></span>  
  
-   <span data-ttu-id="9305c-149">Le `And` opérateur compare les représentations binaires par position binaire (bit).</span><span class="sxs-lookup"><span data-stu-id="9305c-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="9305c-150">Si les deux bits à une position donnée sont 1, 1 est placé à cette position dans le résultat.</span><span class="sxs-lookup"><span data-stu-id="9305c-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="9305c-151">Si un bit est 0, 0 est placé à cette position dans le résultat.</span><span class="sxs-lookup"><span data-stu-id="9305c-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="9305c-152">Dans l’exemple précédent cela fonctionne comme suit :</span><span class="sxs-lookup"><span data-stu-id="9305c-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="9305c-153">011 (3 sous forme binaire)</span><span class="sxs-lookup"><span data-stu-id="9305c-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="9305c-154">101 (5 sous forme binaire)</span><span class="sxs-lookup"><span data-stu-id="9305c-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="9305c-155">001 (le résultat sous forme binaire)</span><span class="sxs-lookup"><span data-stu-id="9305c-155">001 (The result, in binary form)</span></span>  
  
-   <span data-ttu-id="9305c-156">Le résultat est traité comme séparateur décimal.</span><span class="sxs-lookup"><span data-stu-id="9305c-156">The result is treated as decimal.</span></span> <span data-ttu-id="9305c-157">La valeur 001 est la représentation binaire de 1, donc `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="9305c-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="9305c-158">L’opérateur de bits `Or` opération est similaire, à ceci près que 1 est assigné au bit de résultat si une des deux bits comparés est 1.</span><span class="sxs-lookup"><span data-stu-id="9305c-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="9305c-159">`Xor` assigne un 1 au bit de résultat si un seul des bits comparés (mais pas les deux) est 1.</span><span class="sxs-lookup"><span data-stu-id="9305c-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="9305c-160">`Not` accepte un opérande unique et inverse tous les bits, y compris le bit de signe et assigne la valeur au résultat.</span><span class="sxs-lookup"><span data-stu-id="9305c-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="9305c-161">Cela signifie que pour les nombres positifs signés, `Not` retourne toujours une valeur négative et pour les nombres négatifs, `Not` retourne toujours une valeur positive ou nulle.</span><span class="sxs-lookup"><span data-stu-id="9305c-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="9305c-162">Le `AndAlso` et `OrElse` opérateurs ne prennent pas en charge les opérations au niveau du bit.</span><span class="sxs-lookup"><span data-stu-id="9305c-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="9305c-163">Les opérations au niveau du bit peuvent être effectuées sur les types intégraux uniquement.</span><span class="sxs-lookup"><span data-stu-id="9305c-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="9305c-164">Valeurs à virgule flottante doivent être converties en types intégraux avant de poursuivre l’opération au niveau du bit.</span><span class="sxs-lookup"><span data-stu-id="9305c-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9305c-165">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="9305c-165">See also</span></span>
- [<span data-ttu-id="9305c-166">Opérateurs logiques/de bits (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="9305c-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="9305c-167">Expressions booléennes</span><span class="sxs-lookup"><span data-stu-id="9305c-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="9305c-168">Opérateurs arithmétiques en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="9305c-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="9305c-169">Opérateurs de comparaison en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="9305c-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="9305c-170">Opérateurs de concaténation dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="9305c-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="9305c-171">Association efficace d’opérateurs</span><span class="sxs-lookup"><span data-stu-id="9305c-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
