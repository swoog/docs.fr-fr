---
title: "'#ElseIf', '#Else' ou '#End If' doit être précédé d’un '#If' correspondant"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 92a481febe4d2fb4045e7fbb50eee81c9656b189
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/15/2019
ms.locfileid: "58031677"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="33236-102">'#ElseIf', '#Else' ou '#End If' doit être précédé d’un '#If' correspondant</span><span class="sxs-lookup"><span data-stu-id="33236-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="33236-103">`#ElseIf`, `#Else`et `#End If` sont des directives de compilation conditionnelle.</span><span class="sxs-lookup"><span data-stu-id="33236-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="33236-104">`#ElseIf`, `#Else`ou `#End If` n’est pas précédé d’une directive `#If` correspondante.</span><span class="sxs-lookup"><span data-stu-id="33236-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="33236-105">**ID d’erreur :** BC30013</span><span class="sxs-lookup"><span data-stu-id="33236-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="33236-106">Pour corriger cette erreur</span><span class="sxs-lookup"><span data-stu-id="33236-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="33236-107">Vérifiez que la directive `#If` prévue n’est pas séparée de la clause en question par un bloc de compilation conditionnelle intervenant ou une directive `#End If`mal placée.</span><span class="sxs-lookup"><span data-stu-id="33236-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="33236-108">Seule une directive `#Else` étant autorisée dans chaque bloc `#If` , deux directives `#Else` successives provoquent cette erreur.</span><span class="sxs-lookup"><span data-stu-id="33236-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="33236-109">Vérifiez que le caractère de début `#` est présent dans une directive `#If` précédente.</span><span class="sxs-lookup"><span data-stu-id="33236-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="33236-110">Si tout le reste est en ordre, ajoutez une directive `#If` au début du bloc de compilation conditionnelle.</span><span class="sxs-lookup"><span data-stu-id="33236-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="33236-111">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="33236-111">See also</span></span>

- [<span data-ttu-id="33236-112">#If...Then...#Else, directives</span><span class="sxs-lookup"><span data-stu-id="33236-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
