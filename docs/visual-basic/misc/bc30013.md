---
title: "'#ElseIf', '#Else' ou '#End If' doit être précédé d’un '#If' correspondant"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 3e9600c24cccd328265b89d8f4889d6e0735ca05
ms.sourcegitcommit: 0888d7b24f475c346a3f444de8d83ec1ca7cd234
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 12/22/2018
ms.locfileid: "53764005"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="b05d9-102">'#ElseIf', '#Else' ou '#End If' doit être précédé d’un '#If' correspondant</span><span class="sxs-lookup"><span data-stu-id="b05d9-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="b05d9-103">`#ElseIf`, `#Else` et `#End If` sont des directives de compilation conditionnelle.</span><span class="sxs-lookup"><span data-stu-id="b05d9-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="b05d9-104">`#ElseIf`, `#Else`ou `#End If` n’est pas précédé d’une directive `#If` correspondante.</span><span class="sxs-lookup"><span data-stu-id="b05d9-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="b05d9-105">**ID d’erreur :** BC30013</span><span class="sxs-lookup"><span data-stu-id="b05d9-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="b05d9-106">Pour corriger cette erreur</span><span class="sxs-lookup"><span data-stu-id="b05d9-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="b05d9-107">Vérifiez que la directive `#If` prévue n’est pas séparée de la clause en question par un bloc de compilation conditionnelle intervenant ou une directive `#End If`mal placée.</span><span class="sxs-lookup"><span data-stu-id="b05d9-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="b05d9-108">Seule une directive `#Else` étant autorisée dans chaque bloc `#If` , deux directives `#Else` successives provoquent cette erreur.</span><span class="sxs-lookup"><span data-stu-id="b05d9-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="b05d9-109">Vérifiez que le caractère de début `#` est présent dans une directive `#If` précédente.</span><span class="sxs-lookup"><span data-stu-id="b05d9-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="b05d9-110">Si tout le reste est en ordre, ajoutez une directive `#If` au début du bloc de compilation conditionnelle.</span><span class="sxs-lookup"><span data-stu-id="b05d9-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b05d9-111">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="b05d9-111">See Also</span></span>  
 [<span data-ttu-id="b05d9-112">#If...Then...#Else, directives</span><span class="sxs-lookup"><span data-stu-id="b05d9-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
