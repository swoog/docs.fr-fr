---
title: Type de '&lt;variablename&gt;' est ambigu, car les limites de la boucle et la variable étape s’étend pas au même type
ms.date: 07/20/2015
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords:
- BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
ms.openlocfilehash: 3385bab96cea8c460f249aefb87df33a21cf8ed7
ms.sourcegitcommit: 6b308cf6d627d78ee36dbbae8972a310ac7fd6c8
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/23/2019
ms.locfileid: "54747802"
---
# <a name="type-of-ltvariablenamegt-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="0e823-102">Type de '&lt;variablename&gt;' est ambigu, car les limites de la boucle et la variable étape s’étend pas au même type</span><span class="sxs-lookup"><span data-stu-id="0e823-102">Type of '&lt;variablename&gt;' is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>
<span data-ttu-id="0e823-103">Votre code contient une boucle `For...Next` dans laquelle le compilateur ne peut pas déduire un type de données pour la variable de contrôle de boucle, car les conditions suivantes sont remplies :</span><span class="sxs-lookup"><span data-stu-id="0e823-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
-   <span data-ttu-id="0e823-104">Le type de données de la variable de contrôle de boucle n’est pas spécifié avec une clause `As` .</span><span class="sxs-lookup"><span data-stu-id="0e823-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
-   <span data-ttu-id="0e823-105">Les limites de la boucle et l’incrémentation contiennent au moins deux types de données.</span><span class="sxs-lookup"><span data-stu-id="0e823-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
-   <span data-ttu-id="0e823-106">Il existe plusieurs conversions possibles entre les types de données.</span><span class="sxs-lookup"><span data-stu-id="0e823-106">More than one possible conversion exists between the data types.</span></span>  
  
-   <span data-ttu-id="0e823-107">Il n’existe aucun meilleur type parmi les candidats, si bien que le choix du type pour la variable de contrôle de boucle est ambigu.</span><span class="sxs-lookup"><span data-stu-id="0e823-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="0e823-108">Par exemple, la boucle suivante a une limite de boucle de type `Integer` et une autre de type `UInteger`:</span><span class="sxs-lookup"><span data-stu-id="0e823-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="0e823-109">Il existe une conversion possible entre `Integer` et `UInteger`, et une conversion possible entre `UInteger` et `Integer`, mais les deux sont des conversions restrictives, donc aucune ne constitue le meilleur choix.</span><span class="sxs-lookup"><span data-stu-id="0e823-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="0e823-110">Dans l’exemple suivant, une troisième variable de type `Double` est ajoutée.</span><span class="sxs-lookup"><span data-stu-id="0e823-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="0e823-111">`Integer` et `UInteger` ont des conversions étendues standard en `Double`, ce qui fait de la conversion en `Double` le meilleur choix.</span><span class="sxs-lookup"><span data-stu-id="0e823-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="0e823-112">L’inférence de type assigne à la variable de contrôle de boucle `i` le type de données `Double`.</span><span class="sxs-lookup"><span data-stu-id="0e823-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="0e823-113">**ID d’erreur :** BC30983</span><span class="sxs-lookup"><span data-stu-id="0e823-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="0e823-114">Pour corriger cette erreur</span><span class="sxs-lookup"><span data-stu-id="0e823-114">To correct this error</span></span>  
  
-   <span data-ttu-id="0e823-115">Convertissez explicitement l’une des variables en un type vers lequel les autres ont une conversion étendue, par exemple :</span><span class="sxs-lookup"><span data-stu-id="0e823-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```  
    For i = m To CLng(n)  
    ```  
  
-   <span data-ttu-id="0e823-116">Utilisez une clause `As` pour spécifier le type de la variable de contrôle :</span><span class="sxs-lookup"><span data-stu-id="0e823-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```  
    For i As Double = m To n   
    ```  
  
## <a name="see-also"></a><span data-ttu-id="0e823-117">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="0e823-117">See also</span></span>
- [<span data-ttu-id="0e823-118">For...Next (instruction)</span><span class="sxs-lookup"><span data-stu-id="0e823-118">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)
- [<span data-ttu-id="0e823-119">Conversions implicites et explicites</span><span class="sxs-lookup"><span data-stu-id="0e823-119">Implicit and Explicit Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)
- [<span data-ttu-id="0e823-120">Inférence de type local</span><span class="sxs-lookup"><span data-stu-id="0e823-120">Local Type Inference</span></span>](../../visual-basic/programming-guide/language-features/variables/local-type-inference.md)
- [<span data-ttu-id="0e823-121">Option Infer (instruction)</span><span class="sxs-lookup"><span data-stu-id="0e823-121">Option Infer Statement</span></span>](../../visual-basic/language-reference/statements/option-infer-statement.md)
- [<span data-ttu-id="0e823-122">Conversions étendues et restrictives</span><span class="sxs-lookup"><span data-stu-id="0e823-122">Widening and Narrowing Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
